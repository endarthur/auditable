// Generated by atrac — do not edit

export const all = "! ══════════════════════════════════════════════════════════════════════\n! ALAS / ALPACK — dense linear algebra for atra\n!\n! ALAS  (Auditable Linear Algebra Subprograms) — vector/matrix primitives\n! ALPACK (Auditable Linear Algebra PACKage)    — factorizations & solvers\n!\n! all matrices are row-major, flat in linear memory.\n! 2D access: a[i, n, j] where n = number of columns (stride).\n! caller provides all buffers. info[0] = 0 on success.\n! ══════════════════════════════════════════════════════════════════════\n\n! ── ALAS Level 1: vector-vector ─────────────────────────────────────\n\n! x := alpha * x\nsubroutine alas.dscal(x: array f64; n: i32; alpha: f64)\nvar\n  i, n2: i32\n  va, vx: f64x2\nbegin\n  va := f64x2.splat(alpha)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vx := va * vx\n    call v128.store(x, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    x[i] := alpha * x[i]\n  end if\nend\n\n! y := x  (copy x into y)\nsubroutine alas.dcopy(x: array f64; y: array f64; n: i32)\nvar\n  i, n2: i32\n  vx: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    call v128.store(y, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    y[i] := x[i]\n  end if\nend\n\n! y := alpha * x + y\nsubroutine alas.daxpy(x: array f64; y: array f64; n: i32; alpha: f64)\nvar\n  i, n2: i32\n  va, vx, vy: f64x2\nbegin\n  ! scalar version (for reference):\n  !   for i := 0, n\n  !     y[i] := alpha * x[i] + y[i]\n  !   end for\n  va := f64x2.splat(alpha)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vy := f64x2.relaxed_madd(va, vx, vy)\n    call v128.store(y, i, vy)\n  end for\n  ! scalar tail for odd element\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    y[i] := alpha * x[i] + y[i]\n  end if\nend\n\n! returns x^T y  (dot product)\nfunction alas.ddot(x: array f64; y: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vy, vs: f64x2\nbegin\n  ! scalar version:\n  !   alas.ddot := 0.0\n  !   for i := 0, n\n  !     alas.ddot := alas.ddot + x[i] * y[i]\n  !   end for\n  ! NOTE: this loop uses separate mul + add rather than f64x2.relaxed_madd.\n  ! The reduction has a single accumulator vs that's recurrence-dependent\n  ! (vs[k] = vs[k-1] + ...); FMA serializes this dependency chain at FMA\n  ! latency, while separate mul+add lets the CPU overlap multiplication\n  ! with the previous iteration's add. Empirically ~30% faster than FMA\n  ! at 10K-100K on AMD Ryzen AI 9 HX 370. dgemm/dger/dgemv use FMA because\n  ! their inner loops have m×n parallel accumulators (no serialization).\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vs := vs + vx * vy\n  end for\n  alas.ddot := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  ! scalar tail\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.ddot := alas.ddot + x[i] * y[i]\n  end if\nend\n\n! returns ||x||_2  (Euclidean norm)\nfunction alas.dnrm2(x: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vs: f64x2\nbegin\n  ! Same single-accumulator caveat as ddot — see note there.\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vs := vs + vx * vx\n  end for\n  alas.dnrm2 := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.dnrm2 := alas.dnrm2 + x[i] * x[i]\n  end if\n  alas.dnrm2 := sqrt(alas.dnrm2)\nend\n\n! swap x and y\nsubroutine alas.dswap(x: array f64; y: array f64; n: i32)\nvar\n  i, n2: i32\n  t: f64\n  vx, vy: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    call v128.store(x, i, vy)\n    call v128.store(y, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    t := x[i]\n    x[i] := y[i]\n    y[i] := t\n  end if\nend\n\n! returns sum of |x_i|\nfunction alas.dasum(x: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vs: f64x2\nbegin\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vs := vs + f64x2.abs(vx)\n  end for\n  alas.dasum := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.dasum := alas.dasum + abs(x[i])\n  end if\nend\n\n! apply Givens rotation: [x; y] := [c s; -s c] * [x; y]\nsubroutine alas.drot(x: array f64; y: array f64; n: i32; c, s: f64)\nvar\n  i, n2: i32\n  t: f64\n  vc, vs, vx, vy, vt, vy_new: f64x2\nbegin\n  vc := f64x2.splat(c)\n  vs := f64x2.splat(s)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vt := vc * vx + vs * vy\n    vy_new := vc * vy - vs * vx\n    call v128.store(y, i, vy_new)\n    call v128.store(x, i, vt)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    t := c * x[i] + s * y[i]\n    y[i] := c * y[i] - s * x[i]\n    x[i] := t\n  end if\nend\n\n! generate Givens rotation: given (a, b), compute (c, s, r)\n! such that [c s; -s c] * [a; b] = [r; 0]\n! writes c to cs[0], s to cs[1]; returns r\nfunction alas.drotg(a, b: f64; cs: array f64): f64\nvar r, z: f64\nbegin\n  r := sqrt(a * a + b * b)\n  if (r == 0.0) then\n    cs[0] := 1.0\n    cs[1] := 0.0\n    alas.drotg := 0.0\n  else\n    cs[0] := a / r\n    cs[1] := b / r\n    alas.drotg := r\n  end if\nend\n\n! returns index of element with max absolute value\nfunction alas.idamax(x: array f64; n: i32): i32\nvar i: i32; mx: f64; v: f64\nbegin\n  alas.idamax := 0\n  mx := abs(x[0])\n  for i := 1, n\n    v := abs(x[i])\n    if (v > mx) then\n      mx := v\n      alas.idamax := i\n    end if\n  end for\nend\n\n! ── ALAS Level 2: matrix-vector ─────────────────────────────────────\n\n! y := alpha * A * x + beta * y   (general matrix-vector multiply)\n! A is m x n, x has n elements, y has m elements\nsubroutine alas.dgemv(\n  a: array f64; x: array f64; y: array f64;\n  m, n: i32; alpha, beta: f64\n)\nvar\n  i, j, n2, a_row, joff: i32\n  s: f64\n  vs, va, vx: f64x2\nbegin\n  ! Single-accumulator reduction per row → mul+add not FMA (same rationale\n  ! as ddot — FMA serializes the in-row recurrence at FMA latency).\n  n2 := n / 2\n  for i := 0, m\n    vs := f64x2.splat(0.0)\n    a_row := i * n * 8\n    for j := 0, n2\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vx := v128.load(x, j)\n      vs := vs + va * vx\n    end for\n    s := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n    if (n2 * 2 < n) then\n      j := n2 * 2\n      s := s + a[i, n, j] * x[j]\n    end if\n    y[i] := alpha * s + beta * y[i]\n  end for\nend\n\n! solve L*x = b or L^T*x = b  (triangular solve)\n! uplo: 0 = lower, 1 = upper\n! trans: 0 = no transpose, 1 = transpose\n! x is overwritten with the solution (input is b, output is x)\nsubroutine alas.dtrsv(\n  a: array f64; x: array f64;\n  n, uplo, trans: i32\n)\nvar\n  i, j, j2, a_row, joff: i32\n  s: f64\n  acc, va, vx: f64x2\nbegin\n  if (uplo == 0 and trans == 0) then\n    ! lower, no transpose: forward substitution. Inner reads stride-1 → SIMD.\n    for i := 0, n\n      s := x[i]\n      j2 := i / 2\n      acc := f64x2.splat(0.0)\n      a_row := i * n * 8\n      for j := 0, j2\n        joff := j * 16\n        va := v128.load_at(a, a_row + joff)\n        vx := v128.load(x, j)\n        acc := acc + va * vx\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < i) then\n        j := j2 * 2\n        s := s - a[i, n, j] * x[j]\n      end if\n      x[i] := s / a[i, n, i]\n    end for\n  else if (uplo == 0 and trans == 1) then\n    ! lower, transpose (= upper): back sub. Reads a[j,i] stride-n → scalar.\n    for i := n - 1, -1, -1\n      s := x[i]\n      for j := i + 1, n\n        s := s - a[j, n, i] * x[j]\n      end for\n      x[i] := s / a[i, n, i]\n    end for\n  else if (uplo == 1 and trans == 0) then\n    ! upper, no transpose: back substitution. Inner reads stride-1 → SIMD.\n    for i := n - 1, -1, -1\n      s := x[i]\n      j2 := (n - i - 1) / 2\n      acc := f64x2.splat(0.0)\n      a_row := (i * n + i + 1) * 8\n      for j := 0, j2\n        joff := j * 16\n        va := v128.load_at(a, a_row + joff)\n        vx := v128.load_at(x, (i + 1) * 8 + joff)\n        acc := acc + va * vx\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < n - i - 1) then\n        j := i + 1 + j2 * 2\n        s := s - a[i, n, j] * x[j]\n      end if\n      x[i] := s / a[i, n, i]\n    end for\n  else\n    ! upper, transpose (= lower): forward sub. Reads a[j,i] stride-n → scalar.\n    for i := 0, n\n      s := x[i]\n      for j := 0, i\n        s := s - a[j, n, i] * x[j]\n      end for\n      x[i] := s / a[i, n, i]\n    end for\n  end if\nend\n\n! A := A + alpha * x * y^T  (rank-1 update)\n! A is m x n, x has m elements, y has n elements\nsubroutine alas.dger(\n  a: array f64; x: array f64; y: array f64;\n  m, n: i32; alpha: f64\n)\nvar\n  i, j, n2, a_row, joff: i32\n  t: f64\n  tv, vy, va: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, m\n    t := alpha * x[i]\n    tv := f64x2.splat(t)\n    a_row := i * n * 8\n    for j := 0, n2\n      joff := j * 16\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f64x2.relaxed_madd(tv, vy, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (n2 * 2 < n) then\n      j := n2 * 2\n      a[i, n, j] := a[i, n, j] + t * y[j]\n    end if\n  end for\nend\n\n! y := alpha * A * x + beta * y  (symmetric matrix-vector multiply)\n! A is n x n symmetric, only lower triangle is read\nsubroutine alas.dsymv(\n  a: array f64; x: array f64; y: array f64;\n  n: i32; alpha, beta: f64\n)\nvar\n  i, j, j2, a_row, joff: i32\n  s, t: f64\n  vs, va, vat, vx, vy, vxi, valpha: f64x2\nbegin\n  ! scale y by beta\n  for i := 0, n\n    y[i] := beta * y[i]\n  end for\n  ! accumulate: diagonal + off-diagonal (symmetric)\n  ! Each j-iteration reads a[i,j], updates s (reduction) AND y[j] (scatter).\n  ! Both use the same scaled value alpha*a[i,j]. SIMD over j.\n  valpha := f64x2.splat(alpha)\n  for i := 0, n\n    vs := f64x2.splat(0.0)\n    vxi := f64x2.splat(x[i])\n    a_row := i * n * 8\n    j2 := i / 2\n    for j := 0, j2\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vat := valpha * va                      ! alpha * a[i, j..j+1]\n      vx := v128.load(x, j)\n      vs := vs + vat * vx                     ! s += alpha*a[i,j] * x[j]\n      vy := v128.load(y, j)\n      vy := vy + vat * vxi                    ! y[j] += alpha*a[i,j] * x[i]\n      call v128.store(y, j, vy)\n    end for\n    s := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n    if (j2 * 2 < i) then\n      j := j2 * 2\n      t := alpha * a[i, n, j]\n      s := s + t * x[j]\n      y[j] := y[j] + t * x[i]\n    end if\n    y[i] := y[i] + s + alpha * a[i, n, i] * x[i]\n  end for\nend\n\n! A := A + alpha * x * x^T  (symmetric rank-1 update, lower triangle)\n! A is n x n symmetric, only lower triangle is updated\nsubroutine alas.dsyr(\n  a: array f64; x: array f64;\n  n: i32; alpha: f64\n)\nvar\n  i, j, ip1, j2, a_row, joff: i32\n  t: f64\n  tv, vx, va: f64x2\nbegin\n  for i := 0, n\n    t := alpha * x[i]\n    tv := f64x2.splat(t)\n    a_row := i * n * 8\n    ip1 := i + 1\n    j2 := ip1 / 2\n    for j := 0, j2\n      joff := j * 16\n      vx := v128.load(x, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f64x2.relaxed_madd(tv, vx, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (j2 * 2 < ip1) then\n      j := j2 * 2\n      a[i, n, j] := a[i, n, j] + t * x[j]\n    end if\n  end for\nend\n\n! A := A + alpha * (x * y^T + y * x^T)  (symmetric rank-2 update, lower triangle)\n! A is n x n symmetric, only lower triangle is updated\nsubroutine alas.dsyr2(\n  a: array f64; x: array f64; y: array f64;\n  n: i32; alpha: f64\n)\nvar\n  i, j, ip1, j2, a_row, joff: i32\n  tx, ty: f64\n  tvx, tvy, vx, vy, va: f64x2\nbegin\n  for i := 0, n\n    tx := alpha * x[i]\n    ty := alpha * y[i]\n    tvx := f64x2.splat(tx)\n    tvy := f64x2.splat(ty)\n    a_row := i * n * 8\n    ip1 := i + 1\n    j2 := ip1 / 2\n    for j := 0, j2\n      joff := j * 16\n      vx := v128.load(x, j)\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      ! a[i,j] += tx * y[j] + ty * x[j]  via two FMAs\n      va := f64x2.relaxed_madd(tvx, vy, va)\n      va := f64x2.relaxed_madd(tvy, vx, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (j2 * 2 < ip1) then\n      j := j2 * 2\n      a[i, n, j] := a[i, n, j] + tx * y[j] + ty * x[j]\n    end if\n  end for\nend\n\n! x := A * x  or  x := A^T * x  (triangular matrix-vector multiply)\n! uplo: 0 = lower, 1 = upper\n! trans: 0 = no transpose, 1 = transpose\n! diag: 0 = non-unit, 1 = unit diagonal\nsubroutine alas.dtrmv(\n  a: array f64; x: array f64;\n  n, uplo, trans, diag: i32\n)\nvar i, j: i32; s: f64\nbegin\n  if (uplo == 1 and trans == 0) then\n    ! upper, no transpose: forward\n    for i := 0, n\n      s := 0.0\n      if (diag == 1) then\n        s := x[i]\n      else\n        s := a[i, n, i] * x[i]\n      end if\n      for j := i + 1, n\n        s := s + a[i, n, j] * x[j]\n      end for\n      x[i] := s\n    end for\n  else if (uplo == 0 and trans == 0) then\n    ! lower, no transpose: backward\n    for i := n - 1, -1, -1\n      s := 0.0\n      for j := 0, i\n        s := s + a[i, n, j] * x[j]\n      end for\n      if (diag == 1) then\n        s := s + x[i]\n      else\n        s := s + a[i, n, i] * x[i]\n      end if\n      x[i] := s\n    end for\n  else if (uplo == 1 and trans == 1) then\n    ! upper, transpose (= lower): backward\n    for i := n - 1, -1, -1\n      s := 0.0\n      for j := 0, i\n        s := s + a[j, n, i] * x[j]\n      end for\n      if (diag == 1) then\n        s := s + x[i]\n      else\n        s := s + a[i, n, i] * x[i]\n      end if\n      x[i] := s\n    end for\n  else\n    ! lower, transpose (= upper): forward\n    for i := 0, n\n      s := 0.0\n      if (diag == 1) then\n        s := x[i]\n      else\n        s := a[i, n, i] * x[i]\n      end if\n      for j := i + 1, n\n        s := s + a[j, n, i] * x[j]\n      end for\n      x[i] := s\n    end for\n  end if\nend\n\n! ── ALAS Level 3: matrix-matrix ─────────────────────────────────────\n\n! C := alpha * A * B + beta * C  (general matrix-matrix multiply)\n! A is m x k, B is k x n, C is m x n\n!\n! Uses (i, p, j) loop order so the inner loop becomes a daxpy-style update\n! `c[i, j] += t * b[p, j]`. Vectorized with f64x2 + relaxed_madd over the\n! j axis: each iteration computes 2 elements with a single fused-multiply-\n! add. Byte-offset SIMD addressing (v128.load_at) handles odd column count\n! n cleanly — no per-shape branching.\n!\n! beta is handled in a prologue (scale C, or zero it) so the inner loop is\n! a pure accumulation.\n!\n! Note: tried naive cache blocking on (jj, pp) with 64-element blocks —\n! no measurable speedup at 500×500 and a slight regression in some runs.\n! Real cache-friendly dgemm requires packing A/B into block-shaped arrays\n! before the inner kernel, plus three-level blocking sized for L1/L2/L3.\n! That's a substantial separate effort; deferred.\nsubroutine alas.dgemm(\n  a: array f64; b: array f64; c: array f64;\n  m, n, k: i32; alpha, beta: f64\n)\nvar\n  i, j, p, n2, c_row, b_row, joff: i32\n  t: f64\n  tv, vb, vc: f64x2\nbegin\n  ! Phase 1: prepare C.\n  if (beta == 0.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := 0.0\n      end for\n    end for\n  else if (beta /= 1.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := beta * c[i, n, j]\n      end for\n    end for\n  end if\n\n  ! Phase 2: C += alpha * A * B with SIMD inner loop.\n  n2 := n / 2\n  for i := 0, m\n    c_row := i * n * 8         ! byte offset of row i in C\n    for p := 0, k\n      t := alpha * a[i, k, p]\n      tv := f64x2.splat(t)\n      b_row := p * n * 8       ! byte offset of row p in B\n      for j := 0, n2\n        joff := j * 16\n        vb := v128.load_at(b, b_row + joff)\n        vc := v128.load_at(c, c_row + joff)\n        vc := f64x2.relaxed_madd(tv, vb, vc)\n        call v128.store_at(c, c_row + joff, vc)\n      end for\n      ! scalar tail for odd n\n      if (n2 * 2 < n) then\n        j := n2 * 2\n        c[i, n, j] := c[i, n, j] + t * b[p, n, j]\n      end if\n    end for\n  end for\nend\n\n! ══════════════════════════════════════════════════════════════════════\n! ALPACK — factorizations, solvers, eigendecomposition\n! ══════════════════════════════════════════════════════════════════════\n\n! ── Cholesky factorization ──────────────────────────────────────────\n\n! A = L * L^T  (in-place, lower triangle overwritten with L)\n! info[0] = 0 on success, k > 0 if not positive-definite at column k\n!\n! Both inner loops are reductions over k in [0, j) with stride-1 reads\n! along rows of A. SIMD'd with v128 accumulator + scalar tail. Single-\n! accumulator pattern → separate mul+add (no FMA) per ddot rationale.\nsubroutine alpack.dpotrf(a: array f64; n: i32; info: array i32)\nvar\n  i, j, k, j2, ai_base, aj_base, koff: i32\n  s: f64\n  acc, va, vj: f64x2\nbegin\n  info[0] := 0\n  for j := 0, n\n    ! diagonal: s := a[j,j] - sum_{k<j} a[j,k]^2\n    s := a[j, n, j]\n    j2 := j / 2\n    acc := f64x2.splat(0.0)\n    aj_base := j * n * 8\n    for k := 0, j2\n      koff := k * 16\n      vj := v128.load_at(a, aj_base + koff)\n      acc := acc + vj * vj\n    end for\n    s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n    if (j2 * 2 < j) then\n      k := j2 * 2\n      s := s - a[j, n, k] * a[j, n, k]\n    end if\n\n    if (s <= 0.0) then\n      info[0] := j + 1\n      call return()\n    end if\n    a[j, n, j] := sqrt(s)\n\n    ! off-diagonal: s := a[i,j] - sum_{k<j} a[i,k] * a[j,k]\n    for i := j + 1, n\n      s := a[i, n, j]\n      acc := f64x2.splat(0.0)\n      ai_base := i * n * 8\n      for k := 0, j2\n        koff := k * 16\n        va := v128.load_at(a, ai_base + koff)\n        vj := v128.load_at(a, aj_base + koff)\n        acc := acc + va * vj\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < j) then\n        k := j2 * 2\n        s := s - a[i, n, k] * a[j, n, k]\n      end if\n      a[i, n, j] := s / a[j, n, j]\n    end for\n  end for\nend\n\n! solve A*X = B given Cholesky factor L from dpotrf\n! B is overwritten with X.  B is n x nrhs (each column is an RHS)\n!\n! Forward sub (L y = b) reads l[i,j] stride-1 in j → SIMD'd for nrhs=1.\n! Back sub (L^T x = y) reads l[j,i] stride-n in j (column traversal of L)\n! — kept scalar; SIMD would require an on-the-fly transpose.\nsubroutine alpack.dpotrs(\n  l: array f64; b: array f64;\n  n, nrhs: i32\n)\nvar\n  rhs, i, j, j2, l_row, joff: i32\n  s: f64\n  acc, vl, vb: f64x2\nbegin\n  for rhs := 0, nrhs\n    if (nrhs == 1) then\n      ! forward substitution (SIMD nrhs=1 path)\n      for i := 0, n\n        s := b[i]\n        j2 := i / 2\n        acc := f64x2.splat(0.0)\n        l_row := i * n * 8\n        for j := 0, j2\n          joff := j * 16\n          vl := v128.load_at(l, l_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vl * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < i) then\n          j := j2 * 2\n          s := s - l[i, n, j] * b[j]\n        end if\n        b[i] := s / l[i, n, i]\n      end for\n      ! back substitution: column-major access on L → scalar\n      for i := n - 1, -1, -1\n        s := b[i]\n        for j := i + 1, n\n          s := s - l[j, n, i] * b[j]\n        end for\n        b[i] := s / l[i, n, i]\n      end for\n    else\n    ! Multi-RHS scalar path\n    ! forward substitution: L * y = b\n    for i := 0, n\n      s := b[i, nrhs, rhs]\n      for j := 0, i\n        s := s - l[i, n, j] * b[j, nrhs, rhs]\n      end for\n      b[i, nrhs, rhs] := s / l[i, n, i]\n    end for\n    ! back substitution: L^T * x = y\n    for i := n - 1, -1, -1\n      s := b[i, nrhs, rhs]\n      for j := i + 1, n\n        s := s - l[j, n, i] * b[j, nrhs, rhs]\n      end for\n      b[i, nrhs, rhs] := s / l[i, n, i]\n    end for\n    end if\n  end for\nend\n\n! ── LU factorization with partial pivoting ──────────────────────────\n\n! A = P * L * U  (in-place, L below diagonal with unit diagonal, U on+above)\n! ipiv is an n-element i32 array of pivot indices\n! info[0] = 0 on success, k > 0 if U(k,k) is zero\nsubroutine alpack.dgetrf(\n  a: array f64; n: i32;\n  ipiv: array i32; info: array i32\n)\nvar\n  i, j, k, pivot, nrem, nrem2, ai_base, ak_base, joff: i32\n  t, inv, m: f64\n  mv, vk, vi: f64x2\nbegin\n  info[0] := 0\n  for k := 0, n\n    ! find pivot in column k from row k..n-1\n    pivot := k\n    t := abs(a[k, n, k])\n    for i := k + 1, n\n      if (abs(a[i, n, k]) > t) then\n        pivot := i\n        t := abs(a[i, n, k])\n      end if\n    end for\n    ipiv[k] := pivot\n\n    ! swap rows k and pivot\n    if (pivot /= k) then\n      for j := 0, n\n        t := a[k, n, j]\n        a[k, n, j] := a[pivot, n, j]\n        a[pivot, n, j] := t\n      end for\n    end if\n\n    if (a[k, n, k] == 0.0) then\n      info[0] := k + 1\n      call return()\n    end if\n\n    ! eliminate below diagonal: rank-1 update of the trailing (n-k-1)×(n-k-1)\n    ! submatrix using the daxpy/nmadd pattern over the inner j loop.\n    inv := 1.0 / a[k, n, k]\n    nrem := n - k - 1\n    nrem2 := nrem / 2\n    ak_base := (k * n + k + 1) * 8\n    for i := k + 1, n\n      m := a[i, n, k] * inv\n      a[i, n, k] := m\n      mv := f64x2.splat(m)\n      ai_base := (i * n + k + 1) * 8\n      for j := 0, nrem2\n        joff := j * 16\n        vk := v128.load_at(a, ak_base + joff)\n        vi := v128.load_at(a, ai_base + joff)\n        vi := f64x2.relaxed_nmadd(mv, vk, vi)\n        call v128.store_at(a, ai_base + joff, vi)\n      end for\n      ! scalar tail when nrem is odd\n      if (nrem2 * 2 < nrem) then\n        j := k + 1 + nrem2 * 2\n        a[i, n, j] := a[i, n, j] - m * a[k, n, j]\n      end if\n    end for\n  end for\nend\n\n! solve A*X = B given LU factorization from dgetrf\n! B is n x nrhs, overwritten with X\n!\n! For nrhs = 1 (the common case for natra.solve), b is effectively 1D\n! and both lu[i,j] and b[j] are stride-1 in j — the inner reduction can\n! be SIMD'd via a v128 accumulator. For multi-RHS, b accesses are\n! stride-nrhs and the scalar path is used. Same mul+add pattern as ddot\n! (single recurrence-dependent accumulator → no FMA).\nsubroutine alpack.dgetrs(\n  lu: array f64; ipiv: array i32;\n  b: array f64; n, nrhs: i32\n)\nvar\n  rhs, i, j, p, j2, lu_row, joff: i32\n  s, t: f64\n  acc, vlu, vb: f64x2\nbegin\n  for rhs := 0, nrhs\n    ! apply row permutation\n    for i := 0, n\n      p := ipiv[i]\n      if (p /= i) then\n        t := b[i, nrhs, rhs]\n        b[i, nrhs, rhs] := b[p, nrhs, rhs]\n        b[p, nrhs, rhs] := t\n      end if\n    end for\n\n    if (nrhs == 1) then\n      ! SIMD fast path — b stride-1 in j\n      ! forward substitution: s := b[i] - sum_{j<i} lu[i,j] * b[j]\n      for i := 1, n\n        s := b[i]\n        j2 := i / 2\n        acc := f64x2.splat(0.0)\n        lu_row := i * n * 8\n        for j := 0, j2\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vlu * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < i) then\n          j := j2 * 2\n          s := s - lu[i, n, j] * b[j]\n        end if\n        b[i] := s\n      end for\n      ! back substitution\n      for i := n - 1, -1, -1\n        s := b[i]\n        ! reduce over j in [i+1, n)\n        j2 := (n - i - 1) / 2\n        acc := f64x2.splat(0.0)\n        lu_row := (i * n + i + 1) * 8\n        for j := 0, j2\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load_at(b, (i + 1) * 8 + joff)\n          acc := acc + vlu * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < n - i - 1) then\n          j := i + 1 + j2 * 2\n          s := s - lu[i, n, j] * b[j]\n        end if\n        b[i] := s / lu[i, n, i]\n      end for\n    else\n      ! Multi-RHS: scalar path (b stride-nrhs in j)\n      for i := 1, n\n        s := b[i, nrhs, rhs]\n        for j := 0, i\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i, nrhs, rhs]\n        for j := i + 1, n\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s / lu[i, n, i]\n      end for\n    end if\n  end for\nend\n\n! solve A*X = B  (convenience: dgetrf + dgetrs)\n! A is overwritten with LU, B is overwritten with X\n! scratch: i32 array of length n for pivots\nsubroutine alpack.dgesv(\n  a: array f64; b: array f64;\n  n, nrhs: i32;\n  ipiv: array i32; info: array i32\n)\nbegin\n  call alpack.dgetrf(a, n, ipiv, info)\n  if (info[0] /= 0) then\n    call return()\n  end if\n  call alpack.dgetrs(a, ipiv, b, n, nrhs)\nend\n\n! ── Triangular inverse ──────────────────────────────────────────────\n\n! invert a lower triangular matrix in-place\n! info[0] = 0 on success, k > 0 if diagonal element k is zero\nsubroutine alpack.dtrtri(a: array f64; n: i32; info: array i32)\nvar i, j, k: i32; s: f64\nbegin\n  info[0] := 0\n  for j := 0, n\n    if (a[j, n, j] == 0.0) then\n      info[0] := j + 1\n      call return()\n    end if\n    a[j, n, j] := 1.0 / a[j, n, j]\n    for i := j + 1, n\n      s := 0.0\n      for k := j, i\n        s := s - a[i, n, k] * a[k, n, j]\n      end for\n      a[i, n, j] := s / a[i, n, i]\n    end for\n  end for\nend\n\n! ── Eigendecomposition: 3x3 analytical ──────────────────────────────\n\n! eigenvalues of a 3x3 symmetric matrix (analytical, Cardano's method)\n! a is 3x3 symmetric (9 f64), w receives 3 eigenvalues (sorted descending)\nsubroutine alpack.dsyev3(a: array f64; w: array f64)\nvar\n  a00, a01, a02, a11, a12, a22: f64\n  p1, q, p2, p, b00, b11, b22, det_b, half_det: f64\n  phi, r, e1, e2, e3: f64\n  m: f64\n  pi: f64\nbegin\n  pi := 3.14159265358979323846\n\n  a00 := a[0, 3, 0]\n  a01 := a[0, 3, 1]\n  a02 := a[0, 3, 2]\n  a11 := a[1, 3, 1]\n  a12 := a[1, 3, 2]\n  a22 := a[2, 3, 2]\n\n  ! trace / 3\n  m := (a00 + a11 + a22) / 3.0\n\n  ! sum of squares of off-diagonal elements\n  p1 := a01 * a01 + a02 * a02 + a12 * a12\n\n  if (p1 <= 1e-30) then\n    ! diagonal matrix — eigenvalues are the diagonal entries, sort descending\n    e1 := a00\n    e2 := a11\n    e3 := a22\n    ! sort 3 values descending (bubble sort)\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    if (e3 > e2) then\n      r := e2\n      e2 := e3\n      e3 := r\n    end if\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n    call return()\n  end if\n\n  q := m\n  p2 := (a00 - q) * (a00 - q) + (a11 - q) * (a11 - q) + (a22 - q) * (a22 - q) + 2.0 * p1\n  p := sqrt(p2 / 6.0)\n\n  ! B = (A - q*I) / p  — only need determinant\n  b00 := (a00 - q) / p\n  b11 := (a11 - q) / p\n  b22 := (a22 - q) / p\n  ! det(B) for symmetric 3x3\n  det_b := b00 * (b11 * b22 - (a12 / p) * (a12 / p))\n         - (a01 / p) * ((a01 / p) * b22 - (a12 / p) * (a02 / p))\n         + (a02 / p) * ((a01 / p) * (a12 / p) - b11 * (a02 / p))\n  half_det := det_b / 2.0\n\n  ! clamp to [-1, 1] for acos\n  if (half_det <= -1.0) then\n    phi := pi / 3.0\n  else if (half_det >= 1.0) then\n    phi := 0.0\n  else\n    phi := atan2(sqrt(1.0 - half_det * half_det), half_det) / 3.0\n  end if\n\n  ! eigenvalues in descending order\n  e1 := q + 2.0 * p * cos(phi)\n  e3 := q + 2.0 * p * cos(phi + 2.0 * pi / 3.0)\n  e2 := 3.0 * q - e1 - e3\n\n  w[0] := e1\n  w[1] := e2\n  w[2] := e3\nend\n\n! ── Eigendecomposition: 3x3 analytical with eigenvectors ─────────────\n\n! eigenvalues + eigenvectors of a 3x3 symmetric matrix\n! a is 3x3 symmetric (9 f64), w receives 3 eigenvalues (sorted descending),\n! v receives 3x3 eigenvector matrix (columns are eigenvectors, row-major)\n! uses Cardano eigenvalues + cross-product null-space for eigenvectors\nsubroutine alpack.dsyev3v(a: array f64; w: array f64; v: array f64)\nvar\n  a00, a01, a02, a11, a12, a22: f64\n  p1, q, p2, p, b00, b11, b22, det_b, half_det: f64\n  phi, r, e1, e2, e3: f64\n  m: f64\n  pi: f64\n  ! eigenvector computation\n  k, best: i32\n  r0x, r0y, r0z, r1x, r1y, r1z, r2x, r2y, r2z: f64\n  cx, cy, cz: f64\n  n01, n12, n02, nmax, nrm: f64\nbegin\n  pi := 3.14159265358979323846\n\n  a00 := a[0, 3, 0]\n  a01 := a[0, 3, 1]\n  a02 := a[0, 3, 2]\n  a11 := a[1, 3, 1]\n  a12 := a[1, 3, 2]\n  a22 := a[2, 3, 2]\n\n  ! ── compute eigenvalues (same as dsyev3) ──\n  m := (a00 + a11 + a22) / 3.0\n  p1 := a01 * a01 + a02 * a02 + a12 * a12\n\n  if (p1 <= 1e-30) then\n    ! diagonal matrix\n    e1 := a00\n    e2 := a11\n    e3 := a22\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    if (e3 > e2) then\n      r := e2\n      e2 := e3\n      e3 := r\n    end if\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n    ! eigenvectors for diagonal: identity (re-ordered to match eigenvalue order)\n    ! Find which diagonal entry maps to which eigenvalue\n    ! For simplicity with the sorted eigenvalues, compute eigenvectors below\n  else\n    q := m\n    p2 := (a00 - q) * (a00 - q) + (a11 - q) * (a11 - q) + (a22 - q) * (a22 - q) + 2.0 * p1\n    p := sqrt(p2 / 6.0)\n\n    b00 := (a00 - q) / p\n    b11 := (a11 - q) / p\n    b22 := (a22 - q) / p\n    det_b := b00 * (b11 * b22 - (a12 / p) * (a12 / p))\n           - (a01 / p) * ((a01 / p) * b22 - (a12 / p) * (a02 / p))\n           + (a02 / p) * ((a01 / p) * (a12 / p) - b11 * (a02 / p))\n    half_det := det_b / 2.0\n\n    if (half_det <= -1.0) then\n      phi := pi / 3.0\n    else if (half_det >= 1.0) then\n      phi := 0.0\n    else\n      phi := atan2(sqrt(1.0 - half_det * half_det), half_det) / 3.0\n    end if\n\n    e1 := q + 2.0 * p * cos(phi)\n    e3 := q + 2.0 * p * cos(phi + 2.0 * pi / 3.0)\n    e2 := 3.0 * q - e1 - e3\n\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n  end if\n\n  ! ── compute eigenvectors via cross-product null-space ──\n  ! For each eigenvalue w[k], form B = A - w[k]*I, take cross product\n  ! of two rows with largest cross-product norm\n  for k := 0, 3\n    r0x := a00 - w[k]\n    r0y := a01\n    r0z := a02\n    r1x := a01\n    r1y := a11 - w[k]\n    r1z := a12\n    r2x := a02\n    r2y := a12\n    r2z := a22 - w[k]\n\n    ! cross products of row pairs\n    ! r0 x r1\n    cx := r0y * r1z - r0z * r1y\n    cy := r0z * r1x - r0x * r1z\n    cz := r0x * r1y - r0y * r1x\n    n01 := cx * cx + cy * cy + cz * cz\n\n    nmax := n01\n    ! store best cross product\n    v[0, 3, k] := cx\n    v[1, 3, k] := cy\n    v[2, 3, k] := cz\n\n    ! r1 x r2\n    cx := r1y * r2z - r1z * r2y\n    cy := r1z * r2x - r1x * r2z\n    cz := r1x * r2y - r1y * r2x\n    n12 := cx * cx + cy * cy + cz * cz\n    if (n12 > nmax) then\n      nmax := n12\n      v[0, 3, k] := cx\n      v[1, 3, k] := cy\n      v[2, 3, k] := cz\n    end if\n\n    ! r0 x r2\n    cx := r0y * r2z - r0z * r2y\n    cy := r0z * r2x - r0x * r2z\n    cz := r0x * r2y - r0y * r2x\n    n02 := cx * cx + cy * cy + cz * cz\n    if (n02 > nmax) then\n      nmax := n02\n      v[0, 3, k] := cx\n      v[1, 3, k] := cy\n      v[2, 3, k] := cz\n    end if\n\n    ! normalize\n    nrm := sqrt(nmax)\n    if (nrm > 1e-30) then\n      v[0, 3, k] := v[0, 3, k] / nrm\n      v[1, 3, k] := v[1, 3, k] / nrm\n      v[2, 3, k] := v[2, 3, k] / nrm\n    else\n      ! degenerate: use canonical basis vector\n      v[0, 3, k] := 0.0\n      v[1, 3, k] := 0.0\n      v[2, 3, k] := 0.0\n      v[k, 3, k] := 1.0\n    end if\n  end for\nend\n\n! ── Eigendecomposition: general symmetric (Jacobi) ──────────────────\n\n! eigenvalues of an n x n symmetric matrix via cyclic Jacobi rotations\n! a is overwritten with the eigenvector matrix (columns are eigenvectors)\n! w receives n eigenvalues (unsorted)\n! info[0] = 0 on success, 1 if not converged\nsubroutine alpack.dsyev(\n  a: array f64; w: array f64;\n  n: i32; info: array i32\n)\nvar\n  i, j, p, q, iter, maxIter: i32\n  off, thresh, app, aqq, apq: f64\n  tau, t, c, s, tmp: f64\nbegin\n  info[0] := 0\n  maxIter := 100\n\n  ! initialize w to diagonal of a\n  for i := 0, n\n    w[i] := a[i, n, i]\n  end for\n\n  for iter := 0, maxIter\n    ! compute off-diagonal norm\n    off := 0.0\n    for p := 0, n\n      for q := p + 1, n\n        off := off + a[p, n, q] * a[p, n, q]\n      end for\n    end for\n    if (off < 1e-28) then\n      call return()\n    end if\n\n    thresh := if (iter < 4) then 0.2 * off / f64(n * n) else 0.0\n\n    for p := 0, n\n      for q := p + 1, n\n        apq := a[p, n, q]\n        if (abs(apq) <= thresh) then\n          ! skip small elements in early sweeps\n        else\n          app := w[p]\n          aqq := w[q]\n          ! Jacobi rotation angle\n          tau := (aqq - app) / (2.0 * apq)\n          if (tau >= 0.0) then\n            t := 1.0 / (tau + sqrt(1.0 + tau * tau))\n          else\n            t := 0.0 - 1.0 / (0.0 - tau + sqrt(1.0 + tau * tau))\n          end if\n          c := 1.0 / sqrt(1.0 + t * t)\n          s := t * c\n\n          ! update eigenvalues\n          w[p] := app - t * apq\n          w[q] := aqq + t * apq\n          a[p, n, q] := 0.0\n\n          ! rotate rows/columns p and q\n          for i := 0, p\n            tmp := a[i, n, p]\n            a[i, n, p] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := p + 1, q\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := q + 1, n\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[q, n, i]\n            a[q, n, i] := s * tmp + c * a[q, n, i]\n          end for\n        end if\n      end for\n    end for\n  end for\n  info[0] := 1\nend\n\n! ── Eigendecomposition: general symmetric (Jacobi) with eigenvectors ──\n\n! eigenvalues + eigenvectors of an n x n symmetric matrix via cyclic Jacobi\n! a is overwritten (destroyed). w receives n eigenvalues (unsorted).\n! v receives n x n eigenvector matrix (columns are eigenvectors, row-major).\n! info[0] = 0 on success, 1 if not converged\nsubroutine alpack.dsyevv(\n  a: array f64; w: array f64; v: array f64;\n  n: i32; info: array i32\n)\nvar\n  i, j, p, q, iter, maxIter: i32\n  off, thresh, app, aqq, apq: f64\n  tau, t, c, s, tmp: f64\nbegin\n  info[0] := 0\n  maxIter := 100\n\n  ! initialize V = I\n  for i := 0, n\n    for j := 0, n\n      if (i == j) then\n        v[i, n, j] := 1.0\n      else\n        v[i, n, j] := 0.0\n      end if\n    end for\n  end for\n\n  ! initialize w to diagonal of a\n  for i := 0, n\n    w[i] := a[i, n, i]\n  end for\n\n  for iter := 0, maxIter\n    ! compute off-diagonal norm\n    off := 0.0\n    for p := 0, n\n      for q := p + 1, n\n        off := off + a[p, n, q] * a[p, n, q]\n      end for\n    end for\n    if (off < 1e-28) then\n      call return()\n    end if\n\n    thresh := if (iter < 4) then 0.2 * off / f64(n * n) else 0.0\n\n    for p := 0, n\n      for q := p + 1, n\n        apq := a[p, n, q]\n        if (abs(apq) <= thresh) then\n          ! skip small elements in early sweeps\n        else\n          app := w[p]\n          aqq := w[q]\n          tau := (aqq - app) / (2.0 * apq)\n          if (tau >= 0.0) then\n            t := 1.0 / (tau + sqrt(1.0 + tau * tau))\n          else\n            t := 0.0 - 1.0 / (0.0 - tau + sqrt(1.0 + tau * tau))\n          end if\n          c := 1.0 / sqrt(1.0 + t * t)\n          s := t * c\n\n          w[p] := app - t * apq\n          w[q] := aqq + t * apq\n          a[p, n, q] := 0.0\n\n          ! rotate rows/columns p and q\n          for i := 0, p\n            tmp := a[i, n, p]\n            a[i, n, p] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := p + 1, q\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := q + 1, n\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[q, n, i]\n            a[q, n, i] := s * tmp + c * a[q, n, i]\n          end for\n\n          ! accumulate into eigenvector matrix V\n          for i := 0, n\n            tmp := v[i, n, p]\n            v[i, n, p] := c * tmp - s * v[i, n, q]\n            v[i, n, q] := s * tmp + c * v[i, n, q]\n          end for\n        end if\n      end for\n    end for\n  end for\n  info[0] := 1\nend\n! ══════════════════════════════════════════════════════════════════════\n! f32 (single-precision) variants — s-prefixed BLAS naming\n!\n! Mirror the d* routines with f32x4 SIMD (4 lanes per 128-bit) instead of\n! f64x2 (2 lanes). Each scalar/vector type is f32 instead of f64; byte\n! offsets use 4 instead of 8. Per-cycle throughput is approximately 2x\n! higher than the f64 versions on the same hardware.\n!\n! Implemented subset: saxpy, sdot, snrm2, scopy, sscal, sgemv, sger,\n! sgemm, sgetrf, sgetrs, sgesv. Other f32 BLAS/LAPACK routines (spotrf,\n! spotrs, etc.) can be added as needed; the porting is mechanical.\n! ══════════════════════════════════════════════════════════════════════\n\n! ── ALAS Level 1 (f32) ────────────────────────────────────────────────\n\nsubroutine alas.sscal(x: array f32; n: i32; alpha: f32)\nvar\n  i, n4: i32\n  va, vx: f32x4\nbegin\n  va := f32x4.splat(alpha)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vx := va * vx\n    call v128.store(x, i, vx)\n  end for\n  for i := n4 * 4, n\n    x[i] := alpha * x[i]\n  end for\nend\n\nsubroutine alas.scopy(x: array f32; y: array f32; n: i32)\nvar\n  i, n4: i32\n  vx: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    call v128.store(y, i, vx)\n  end for\n  for i := n4 * 4, n\n    y[i] := x[i]\n  end for\nend\n\nsubroutine alas.saxpy(x: array f32; y: array f32; n: i32; alpha: f32)\nvar\n  i, n4: i32\n  va, vx, vy: f32x4\nbegin\n  va := f32x4.splat(alpha)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vy := f32x4.relaxed_madd(va, vx, vy)\n    call v128.store(y, i, vy)\n  end for\n  for i := n4 * 4, n\n    y[i] := alpha * x[i] + y[i]\n  end for\nend\n\nfunction alas.sdot(x: array f32; y: array f32; n: i32): f32\nvar\n  i, n4: i32\n  vx, vy, vs: f32x4\nbegin\n  vs := f32x4.splat(0.0)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vs := vs + vx * vy\n  end for\n  alas.sdot := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n  for i := n4 * 4, n\n    alas.sdot := alas.sdot + x[i] * y[i]\n  end for\nend\n\nfunction alas.snrm2(x: array f32; n: i32): f32\nvar\n  i, n4: i32\n  vx, vs: f32x4\nbegin\n  vs := f32x4.splat(0.0)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vs := vs + vx * vx\n  end for\n  alas.snrm2 := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n  for i := n4 * 4, n\n    alas.snrm2 := alas.snrm2 + x[i] * x[i]\n  end for\n  alas.snrm2 := sqrt(alas.snrm2)\nend\n\n! ── ALAS Level 2 (f32) ────────────────────────────────────────────────\n\nsubroutine alas.sgemv(\n  a: array f32; x: array f32; y: array f32;\n  m, n: i32; alpha, beta: f32\n)\nvar\n  i, j, n4, a_row, joff: i32\n  s: f32\n  vs, va, vx: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, m\n    vs := f32x4.splat(0.0)\n    a_row := i * n * 4\n    for j := 0, n4\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vx := v128.load(x, j)\n      vs := vs + va * vx\n    end for\n    s := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n    for j := n4 * 4, n\n      s := s + a[i, n, j] * x[j]\n    end for\n    y[i] := alpha * s + beta * y[i]\n  end for\nend\n\nsubroutine alas.sger(\n  a: array f32; x: array f32; y: array f32;\n  m, n: i32; alpha: f32\n)\nvar\n  i, j, n4, a_row, joff: i32\n  t: f32\n  tv, vy, va: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, m\n    t := alpha * x[i]\n    tv := f32x4.splat(t)\n    a_row := i * n * 4\n    for j := 0, n4\n      joff := j * 16\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f32x4.relaxed_madd(tv, vy, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    for j := n4 * 4, n\n      a[i, n, j] := a[i, n, j] + t * y[j]\n    end for\n  end for\nend\n\n! ── ALAS Level 3 (f32) ────────────────────────────────────────────────\n\nsubroutine alas.sgemm(\n  a: array f32; b: array f32; c: array f32;\n  m, n, k: i32; alpha, beta: f32\n)\nvar\n  i, j, p, n4, c_row, b_row, joff: i32\n  t: f32\n  tv, vb, vc: f32x4\nbegin\n  if (beta == 0.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := 0.0\n      end for\n    end for\n  else if (beta /= 1.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := beta * c[i, n, j]\n      end for\n    end for\n  end if\n\n  n4 := n / 4\n  for i := 0, m\n    c_row := i * n * 4\n    for p := 0, k\n      t := alpha * a[i, k, p]\n      tv := f32x4.splat(t)\n      b_row := p * n * 4\n      for j := 0, n4\n        joff := j * 16\n        vb := v128.load_at(b, b_row + joff)\n        vc := v128.load_at(c, c_row + joff)\n        vc := f32x4.relaxed_madd(tv, vb, vc)\n        call v128.store_at(c, c_row + joff, vc)\n      end for\n      for j := n4 * 4, n\n        c[i, n, j] := c[i, n, j] + t * b[p, n, j]\n      end for\n    end for\n  end for\nend\n\n! ── ALPACK (f32) ──────────────────────────────────────────────────────\n\nsubroutine alpack.sgetrf(\n  a: array f32; n: i32;\n  ipiv: array i32; info: array i32\n)\nvar\n  i, j, k, pivot, nrem, nrem4, ai_base, ak_base, joff: i32\n  t, inv, m, v: f32\n  mv, vk, vi: f32x4\nbegin\n  info[0] := 0\n  for k := 0, n\n    pivot := k\n    v := a[k, n, k]\n    t := abs(v)\n    for i := k + 1, n\n      v := a[i, n, k]\n      v := abs(v)\n      if (v > t) then\n        pivot := i\n        t := v\n      end if\n    end for\n    ipiv[k] := pivot\n\n    if (pivot /= k) then\n      for j := 0, n\n        t := a[k, n, j]\n        a[k, n, j] := a[pivot, n, j]\n        a[pivot, n, j] := t\n      end for\n    end if\n\n    if (a[k, n, k] == 0.0) then\n      info[0] := k + 1\n      call return()\n    end if\n\n    inv := 1.0 / a[k, n, k]\n    nrem := n - k - 1\n    nrem4 := nrem / 4\n    ak_base := (k * n + k + 1) * 4\n    for i := k + 1, n\n      m := a[i, n, k] * inv\n      a[i, n, k] := m\n      mv := f32x4.splat(m)\n      ai_base := (i * n + k + 1) * 4\n      for j := 0, nrem4\n        joff := j * 16\n        vk := v128.load_at(a, ak_base + joff)\n        vi := v128.load_at(a, ai_base + joff)\n        vi := f32x4.relaxed_nmadd(mv, vk, vi)\n        call v128.store_at(a, ai_base + joff, vi)\n      end for\n      for j := k + 1 + nrem4 * 4, n\n        a[i, n, j] := a[i, n, j] - m * a[k, n, j]\n      end for\n    end for\n  end for\nend\n\nsubroutine alpack.sgetrs(\n  lu: array f32; ipiv: array i32;\n  b: array f32; n, nrhs: i32\n)\nvar\n  rhs, i, j, p, j4, lu_row, joff: i32\n  s, t: f32\n  acc, vlu, vb: f32x4\nbegin\n  for rhs := 0, nrhs\n    for i := 0, n\n      p := ipiv[i]\n      if (p /= i) then\n        t := b[i, nrhs, rhs]\n        b[i, nrhs, rhs] := b[p, nrhs, rhs]\n        b[p, nrhs, rhs] := t\n      end if\n    end for\n\n    if (nrhs == 1) then\n      for i := 1, n\n        s := b[i]\n        j4 := i / 4\n        acc := f32x4.splat(0.0)\n        lu_row := i * n * 4\n        for j := 0, j4\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vlu * vb\n        end for\n        s := s - f32x4.extract_lane(acc, 0) - f32x4.extract_lane(acc, 1) - f32x4.extract_lane(acc, 2) - f32x4.extract_lane(acc, 3)\n        for j := j4 * 4, i\n          s := s - lu[i, n, j] * b[j]\n        end for\n        b[i] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i]\n        j4 := (n - i - 1) / 4\n        acc := f32x4.splat(0.0)\n        lu_row := (i * n + i + 1) * 4\n        for j := 0, j4\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load_at(b, (i + 1) * 4 + joff)\n          acc := acc + vlu * vb\n        end for\n        s := s - f32x4.extract_lane(acc, 0) - f32x4.extract_lane(acc, 1) - f32x4.extract_lane(acc, 2) - f32x4.extract_lane(acc, 3)\n        for j := i + 1 + j4 * 4, n\n          s := s - lu[i, n, j] * b[j]\n        end for\n        b[i] := s / lu[i, n, i]\n      end for\n    else\n      for i := 1, n\n        s := b[i, nrhs, rhs]\n        for j := 0, i\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i, nrhs, rhs]\n        for j := i + 1, n\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s / lu[i, n, i]\n      end for\n    end if\n  end for\nend\n\nsubroutine alpack.sgesv(\n  a: array f32; b: array f32;\n  n, nrhs: i32;\n  ipiv: array i32; info: array i32\n)\nbegin\n  call alpack.sgetrf(a, n, ipiv, info)\n  if (info[0] /= 0) then\n    call return()\n  end if\n  call alpack.sgetrs(a, ipiv, b, n, nrhs)\nend\n";

export const alas_dscal = "subroutine alas.dscal(x: array f64; n: i32; alpha: f64)\nvar\n  i, n2: i32\n  va, vx: f64x2\nbegin\n  va := f64x2.splat(alpha)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vx := va * vx\n    call v128.store(x, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    x[i] := alpha * x[i]\n  end if\nend";

export const alas_dcopy = "subroutine alas.dcopy(x: array f64; y: array f64; n: i32)\nvar\n  i, n2: i32\n  vx: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    call v128.store(y, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    y[i] := x[i]\n  end if\nend";

export const alas_daxpy = "subroutine alas.daxpy(x: array f64; y: array f64; n: i32; alpha: f64)\nvar\n  i, n2: i32\n  va, vx, vy: f64x2\nbegin\n  ! scalar version (for reference):\n  !   for i := 0, n\n  !     y[i] := alpha * x[i] + y[i]\n  !   end for\n  va := f64x2.splat(alpha)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vy := f64x2.relaxed_madd(va, vx, vy)\n    call v128.store(y, i, vy)\n  end for\n  ! scalar tail for odd element\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    y[i] := alpha * x[i] + y[i]\n  end if\nend";

export const alas_ddot = "function alas.ddot(x: array f64; y: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vy, vs: f64x2\nbegin\n  ! scalar version:\n  !   alas.ddot := 0.0\n  !   for i := 0, n\n  !     alas.ddot := alas.ddot + x[i] * y[i]\n  !   end for\n  ! NOTE: this loop uses separate mul + add rather than f64x2.relaxed_madd.\n  ! The reduction has a single accumulator vs that's recurrence-dependent\n  ! (vs[k] = vs[k-1] + ...); FMA serializes this dependency chain at FMA\n  ! latency, while separate mul+add lets the CPU overlap multiplication\n  ! with the previous iteration's add. Empirically ~30% faster than FMA\n  ! at 10K-100K on AMD Ryzen AI 9 HX 370. dgemm/dger/dgemv use FMA because\n  ! their inner loops have m×n parallel accumulators (no serialization).\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vs := vs + vx * vy\n  end for\n  alas.ddot := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  ! scalar tail\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.ddot := alas.ddot + x[i] * y[i]\n  end if\nend";

export const alas_dnrm2 = "function alas.dnrm2(x: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vs: f64x2\nbegin\n  ! Same single-accumulator caveat as ddot — see note there.\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vs := vs + vx * vx\n  end for\n  alas.dnrm2 := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.dnrm2 := alas.dnrm2 + x[i] * x[i]\n  end if\n  alas.dnrm2 := sqrt(alas.dnrm2)\nend";

export const alas_dswap = "subroutine alas.dswap(x: array f64; y: array f64; n: i32)\nvar\n  i, n2: i32\n  t: f64\n  vx, vy: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    call v128.store(x, i, vy)\n    call v128.store(y, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    t := x[i]\n    x[i] := y[i]\n    y[i] := t\n  end if\nend";

export const alas_dasum = "function alas.dasum(x: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vs: f64x2\nbegin\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vs := vs + f64x2.abs(vx)\n  end for\n  alas.dasum := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.dasum := alas.dasum + abs(x[i])\n  end if\nend";

export const alas_drot = "subroutine alas.drot(x: array f64; y: array f64; n: i32; c, s: f64)\nvar\n  i, n2: i32\n  t: f64\n  vc, vs, vx, vy, vt, vy_new: f64x2\nbegin\n  vc := f64x2.splat(c)\n  vs := f64x2.splat(s)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vt := vc * vx + vs * vy\n    vy_new := vc * vy - vs * vx\n    call v128.store(y, i, vy_new)\n    call v128.store(x, i, vt)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    t := c * x[i] + s * y[i]\n    y[i] := c * y[i] - s * x[i]\n    x[i] := t\n  end if\nend";

export const alas_drotg = "function alas.drotg(a, b: f64; cs: array f64): f64\nvar r, z: f64\nbegin\n  r := sqrt(a * a + b * b)\n  if (r == 0.0) then\n    cs[0] := 1.0\n    cs[1] := 0.0\n    alas.drotg := 0.0\n  else\n    cs[0] := a / r\n    cs[1] := b / r\n    alas.drotg := r\n  end if\nend";

export const alas_idamax = "function alas.idamax(x: array f64; n: i32): i32\nvar i: i32; mx: f64; v: f64\nbegin\n  alas.idamax := 0\n  mx := abs(x[0])\n  for i := 1, n\n    v := abs(x[i])\n    if (v > mx) then\n      mx := v\n      alas.idamax := i\n    end if\n  end for\nend";

export const alas_dgemv = "subroutine alas.dgemv(\n  a: array f64; x: array f64; y: array f64;\n  m, n: i32; alpha, beta: f64\n)\nvar\n  i, j, n2, a_row, joff: i32\n  s: f64\n  vs, va, vx: f64x2\nbegin\n  ! Single-accumulator reduction per row → mul+add not FMA (same rationale\n  ! as ddot — FMA serializes the in-row recurrence at FMA latency).\n  n2 := n / 2\n  for i := 0, m\n    vs := f64x2.splat(0.0)\n    a_row := i * n * 8\n    for j := 0, n2\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vx := v128.load(x, j)\n      vs := vs + va * vx\n    end for\n    s := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n    if (n2 * 2 < n) then\n      j := n2 * 2\n      s := s + a[i, n, j] * x[j]\n    end if\n    y[i] := alpha * s + beta * y[i]\n  end for\nend";

export const alas_dtrsv = "subroutine alas.dtrsv(\n  a: array f64; x: array f64;\n  n, uplo, trans: i32\n)\nvar\n  i, j, j2, a_row, joff: i32\n  s: f64\n  acc, va, vx: f64x2\nbegin\n  if (uplo == 0 and trans == 0) then\n    ! lower, no transpose: forward substitution. Inner reads stride-1 → SIMD.\n    for i := 0, n\n      s := x[i]\n      j2 := i / 2\n      acc := f64x2.splat(0.0)\n      a_row := i * n * 8\n      for j := 0, j2\n        joff := j * 16\n        va := v128.load_at(a, a_row + joff)\n        vx := v128.load(x, j)\n        acc := acc + va * vx\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < i) then\n        j := j2 * 2\n        s := s - a[i, n, j] * x[j]\n      end if\n      x[i] := s / a[i, n, i]\n    end for\n  else if (uplo == 0 and trans == 1) then\n    ! lower, transpose (= upper): back sub. Reads a[j,i] stride-n → scalar.\n    for i := n - 1, -1, -1\n      s := x[i]\n      for j := i + 1, n\n        s := s - a[j, n, i] * x[j]\n      end for\n      x[i] := s / a[i, n, i]\n    end for\n  else if (uplo == 1 and trans == 0) then\n    ! upper, no transpose: back substitution. Inner reads stride-1 → SIMD.\n    for i := n - 1, -1, -1\n      s := x[i]\n      j2 := (n - i - 1) / 2\n      acc := f64x2.splat(0.0)\n      a_row := (i * n + i + 1) * 8\n      for j := 0, j2\n        joff := j * 16\n        va := v128.load_at(a, a_row + joff)\n        vx := v128.load_at(x, (i + 1) * 8 + joff)\n        acc := acc + va * vx\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < n - i - 1) then\n        j := i + 1 + j2 * 2\n        s := s - a[i, n, j] * x[j]\n      end if\n      x[i] := s / a[i, n, i]\n    end for\n  else\n    ! upper, transpose (= lower): forward sub. Reads a[j,i] stride-n → scalar.\n    for i := 0, n\n      s := x[i]\n      for j := 0, i\n        s := s - a[j, n, i] * x[j]\n      end for\n      x[i] := s / a[i, n, i]\n    end for\n  end if\nend";

export const alas_dger = "subroutine alas.dger(\n  a: array f64; x: array f64; y: array f64;\n  m, n: i32; alpha: f64\n)\nvar\n  i, j, n2, a_row, joff: i32\n  t: f64\n  tv, vy, va: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, m\n    t := alpha * x[i]\n    tv := f64x2.splat(t)\n    a_row := i * n * 8\n    for j := 0, n2\n      joff := j * 16\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f64x2.relaxed_madd(tv, vy, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (n2 * 2 < n) then\n      j := n2 * 2\n      a[i, n, j] := a[i, n, j] + t * y[j]\n    end if\n  end for\nend";

export const alas_dsymv = "subroutine alas.dsymv(\n  a: array f64; x: array f64; y: array f64;\n  n: i32; alpha, beta: f64\n)\nvar\n  i, j, j2, a_row, joff: i32\n  s, t: f64\n  vs, va, vat, vx, vy, vxi, valpha: f64x2\nbegin\n  ! scale y by beta\n  for i := 0, n\n    y[i] := beta * y[i]\n  end for\n  ! accumulate: diagonal + off-diagonal (symmetric)\n  ! Each j-iteration reads a[i,j], updates s (reduction) AND y[j] (scatter).\n  ! Both use the same scaled value alpha*a[i,j]. SIMD over j.\n  valpha := f64x2.splat(alpha)\n  for i := 0, n\n    vs := f64x2.splat(0.0)\n    vxi := f64x2.splat(x[i])\n    a_row := i * n * 8\n    j2 := i / 2\n    for j := 0, j2\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vat := valpha * va                      ! alpha * a[i, j..j+1]\n      vx := v128.load(x, j)\n      vs := vs + vat * vx                     ! s += alpha*a[i,j] * x[j]\n      vy := v128.load(y, j)\n      vy := vy + vat * vxi                    ! y[j] += alpha*a[i,j] * x[i]\n      call v128.store(y, j, vy)\n    end for\n    s := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n    if (j2 * 2 < i) then\n      j := j2 * 2\n      t := alpha * a[i, n, j]\n      s := s + t * x[j]\n      y[j] := y[j] + t * x[i]\n    end if\n    y[i] := y[i] + s + alpha * a[i, n, i] * x[i]\n  end for\nend";

export const alas_dsyr = "subroutine alas.dsyr(\n  a: array f64; x: array f64;\n  n: i32; alpha: f64\n)\nvar\n  i, j, ip1, j2, a_row, joff: i32\n  t: f64\n  tv, vx, va: f64x2\nbegin\n  for i := 0, n\n    t := alpha * x[i]\n    tv := f64x2.splat(t)\n    a_row := i * n * 8\n    ip1 := i + 1\n    j2 := ip1 / 2\n    for j := 0, j2\n      joff := j * 16\n      vx := v128.load(x, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f64x2.relaxed_madd(tv, vx, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (j2 * 2 < ip1) then\n      j := j2 * 2\n      a[i, n, j] := a[i, n, j] + t * x[j]\n    end if\n  end for\nend";

export const alas_dsyr2 = "subroutine alas.dsyr2(\n  a: array f64; x: array f64; y: array f64;\n  n: i32; alpha: f64\n)\nvar\n  i, j, ip1, j2, a_row, joff: i32\n  tx, ty: f64\n  tvx, tvy, vx, vy, va: f64x2\nbegin\n  for i := 0, n\n    tx := alpha * x[i]\n    ty := alpha * y[i]\n    tvx := f64x2.splat(tx)\n    tvy := f64x2.splat(ty)\n    a_row := i * n * 8\n    ip1 := i + 1\n    j2 := ip1 / 2\n    for j := 0, j2\n      joff := j * 16\n      vx := v128.load(x, j)\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      ! a[i,j] += tx * y[j] + ty * x[j]  via two FMAs\n      va := f64x2.relaxed_madd(tvx, vy, va)\n      va := f64x2.relaxed_madd(tvy, vx, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (j2 * 2 < ip1) then\n      j := j2 * 2\n      a[i, n, j] := a[i, n, j] + tx * y[j] + ty * x[j]\n    end if\n  end for\nend";

export const alas_dtrmv = "subroutine alas.dtrmv(\n  a: array f64; x: array f64;\n  n, uplo, trans, diag: i32\n)\nvar i, j: i32; s: f64\nbegin\n  if (uplo == 1 and trans == 0) then\n    ! upper, no transpose: forward\n    for i := 0, n\n      s := 0.0\n      if (diag == 1) then\n        s := x[i]\n      else\n        s := a[i, n, i] * x[i]\n      end if\n      for j := i + 1, n\n        s := s + a[i, n, j] * x[j]\n      end for\n      x[i] := s\n    end for\n  else if (uplo == 0 and trans == 0) then\n    ! lower, no transpose: backward\n    for i := n - 1, -1, -1\n      s := 0.0\n      for j := 0, i\n        s := s + a[i, n, j] * x[j]\n      end for\n      if (diag == 1) then\n        s := s + x[i]\n      else\n        s := s + a[i, n, i] * x[i]\n      end if\n      x[i] := s\n    end for\n  else if (uplo == 1 and trans == 1) then\n    ! upper, transpose (= lower): backward\n    for i := n - 1, -1, -1\n      s := 0.0\n      for j := 0, i\n        s := s + a[j, n, i] * x[j]\n      end for\n      if (diag == 1) then\n        s := s + x[i]\n      else\n        s := s + a[i, n, i] * x[i]\n      end if\n      x[i] := s\n    end for\n  else\n    ! lower, transpose (= upper): forward\n    for i := 0, n\n      s := 0.0\n      if (diag == 1) then\n        s := x[i]\n      else\n        s := a[i, n, i] * x[i]\n      end if\n      for j := i + 1, n\n        s := s + a[j, n, i] * x[j]\n      end for\n      x[i] := s\n    end for\n  end if\nend";

export const alas_dgemm = "subroutine alas.dgemm(\n  a: array f64; b: array f64; c: array f64;\n  m, n, k: i32; alpha, beta: f64\n)\nvar\n  i, j, p, n2, c_row, b_row, joff: i32\n  t: f64\n  tv, vb, vc: f64x2\nbegin\n  ! Phase 1: prepare C.\n  if (beta == 0.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := 0.0\n      end for\n    end for\n  else if (beta /= 1.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := beta * c[i, n, j]\n      end for\n    end for\n  end if\n\n  ! Phase 2: C += alpha * A * B with SIMD inner loop.\n  n2 := n / 2\n  for i := 0, m\n    c_row := i * n * 8         ! byte offset of row i in C\n    for p := 0, k\n      t := alpha * a[i, k, p]\n      tv := f64x2.splat(t)\n      b_row := p * n * 8       ! byte offset of row p in B\n      for j := 0, n2\n        joff := j * 16\n        vb := v128.load_at(b, b_row + joff)\n        vc := v128.load_at(c, c_row + joff)\n        vc := f64x2.relaxed_madd(tv, vb, vc)\n        call v128.store_at(c, c_row + joff, vc)\n      end for\n      ! scalar tail for odd n\n      if (n2 * 2 < n) then\n        j := n2 * 2\n        c[i, n, j] := c[i, n, j] + t * b[p, n, j]\n      end if\n    end for\n  end for\nend";

export const alpack_dpotrf = "subroutine alpack.dpotrf(a: array f64; n: i32; info: array i32)\nvar\n  i, j, k, j2, ai_base, aj_base, koff: i32\n  s: f64\n  acc, va, vj: f64x2\nbegin\n  info[0] := 0\n  for j := 0, n\n    ! diagonal: s := a[j,j] - sum_{k<j} a[j,k]^2\n    s := a[j, n, j]\n    j2 := j / 2\n    acc := f64x2.splat(0.0)\n    aj_base := j * n * 8\n    for k := 0, j2\n      koff := k * 16\n      vj := v128.load_at(a, aj_base + koff)\n      acc := acc + vj * vj\n    end for\n    s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n    if (j2 * 2 < j) then\n      k := j2 * 2\n      s := s - a[j, n, k] * a[j, n, k]\n    end if\n\n    if (s <= 0.0) then\n      info[0] := j + 1\n      call return()\n    end if\n    a[j, n, j] := sqrt(s)\n\n    ! off-diagonal: s := a[i,j] - sum_{k<j} a[i,k] * a[j,k]\n    for i := j + 1, n\n      s := a[i, n, j]\n      acc := f64x2.splat(0.0)\n      ai_base := i * n * 8\n      for k := 0, j2\n        koff := k * 16\n        va := v128.load_at(a, ai_base + koff)\n        vj := v128.load_at(a, aj_base + koff)\n        acc := acc + va * vj\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < j) then\n        k := j2 * 2\n        s := s - a[i, n, k] * a[j, n, k]\n      end if\n      a[i, n, j] := s / a[j, n, j]\n    end for\n  end for\nend";

export const alpack_dpotrs = "subroutine alpack.dpotrs(\n  l: array f64; b: array f64;\n  n, nrhs: i32\n)\nvar\n  rhs, i, j, j2, l_row, joff: i32\n  s: f64\n  acc, vl, vb: f64x2\nbegin\n  for rhs := 0, nrhs\n    if (nrhs == 1) then\n      ! forward substitution (SIMD nrhs=1 path)\n      for i := 0, n\n        s := b[i]\n        j2 := i / 2\n        acc := f64x2.splat(0.0)\n        l_row := i * n * 8\n        for j := 0, j2\n          joff := j * 16\n          vl := v128.load_at(l, l_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vl * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < i) then\n          j := j2 * 2\n          s := s - l[i, n, j] * b[j]\n        end if\n        b[i] := s / l[i, n, i]\n      end for\n      ! back substitution: column-major access on L → scalar\n      for i := n - 1, -1, -1\n        s := b[i]\n        for j := i + 1, n\n          s := s - l[j, n, i] * b[j]\n        end for\n        b[i] := s / l[i, n, i]\n      end for\n    else\n    ! Multi-RHS scalar path\n    ! forward substitution: L * y = b\n    for i := 0, n\n      s := b[i, nrhs, rhs]\n      for j := 0, i\n        s := s - l[i, n, j] * b[j, nrhs, rhs]\n      end for\n      b[i, nrhs, rhs] := s / l[i, n, i]\n    end for\n    ! back substitution: L^T * x = y\n    for i := n - 1, -1, -1\n      s := b[i, nrhs, rhs]\n      for j := i + 1, n\n        s := s - l[j, n, i] * b[j, nrhs, rhs]\n      end for\n      b[i, nrhs, rhs] := s / l[i, n, i]\n    end for\n    end if\n  end for\nend";

export const alpack_dgetrf = "subroutine alpack.dgetrf(\n  a: array f64; n: i32;\n  ipiv: array i32; info: array i32\n)\nvar\n  i, j, k, pivot, nrem, nrem2, ai_base, ak_base, joff: i32\n  t, inv, m: f64\n  mv, vk, vi: f64x2\nbegin\n  info[0] := 0\n  for k := 0, n\n    ! find pivot in column k from row k..n-1\n    pivot := k\n    t := abs(a[k, n, k])\n    for i := k + 1, n\n      if (abs(a[i, n, k]) > t) then\n        pivot := i\n        t := abs(a[i, n, k])\n      end if\n    end for\n    ipiv[k] := pivot\n\n    ! swap rows k and pivot\n    if (pivot /= k) then\n      for j := 0, n\n        t := a[k, n, j]\n        a[k, n, j] := a[pivot, n, j]\n        a[pivot, n, j] := t\n      end for\n    end if\n\n    if (a[k, n, k] == 0.0) then\n      info[0] := k + 1\n      call return()\n    end if\n\n    ! eliminate below diagonal: rank-1 update of the trailing (n-k-1)×(n-k-1)\n    ! submatrix using the daxpy/nmadd pattern over the inner j loop.\n    inv := 1.0 / a[k, n, k]\n    nrem := n - k - 1\n    nrem2 := nrem / 2\n    ak_base := (k * n + k + 1) * 8\n    for i := k + 1, n\n      m := a[i, n, k] * inv\n      a[i, n, k] := m\n      mv := f64x2.splat(m)\n      ai_base := (i * n + k + 1) * 8\n      for j := 0, nrem2\n        joff := j * 16\n        vk := v128.load_at(a, ak_base + joff)\n        vi := v128.load_at(a, ai_base + joff)\n        vi := f64x2.relaxed_nmadd(mv, vk, vi)\n        call v128.store_at(a, ai_base + joff, vi)\n      end for\n      ! scalar tail when nrem is odd\n      if (nrem2 * 2 < nrem) then\n        j := k + 1 + nrem2 * 2\n        a[i, n, j] := a[i, n, j] - m * a[k, n, j]\n      end if\n    end for\n  end for\nend";

export const alpack_dgetrs = "subroutine alpack.dgetrs(\n  lu: array f64; ipiv: array i32;\n  b: array f64; n, nrhs: i32\n)\nvar\n  rhs, i, j, p, j2, lu_row, joff: i32\n  s, t: f64\n  acc, vlu, vb: f64x2\nbegin\n  for rhs := 0, nrhs\n    ! apply row permutation\n    for i := 0, n\n      p := ipiv[i]\n      if (p /= i) then\n        t := b[i, nrhs, rhs]\n        b[i, nrhs, rhs] := b[p, nrhs, rhs]\n        b[p, nrhs, rhs] := t\n      end if\n    end for\n\n    if (nrhs == 1) then\n      ! SIMD fast path — b stride-1 in j\n      ! forward substitution: s := b[i] - sum_{j<i} lu[i,j] * b[j]\n      for i := 1, n\n        s := b[i]\n        j2 := i / 2\n        acc := f64x2.splat(0.0)\n        lu_row := i * n * 8\n        for j := 0, j2\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vlu * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < i) then\n          j := j2 * 2\n          s := s - lu[i, n, j] * b[j]\n        end if\n        b[i] := s\n      end for\n      ! back substitution\n      for i := n - 1, -1, -1\n        s := b[i]\n        ! reduce over j in [i+1, n)\n        j2 := (n - i - 1) / 2\n        acc := f64x2.splat(0.0)\n        lu_row := (i * n + i + 1) * 8\n        for j := 0, j2\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load_at(b, (i + 1) * 8 + joff)\n          acc := acc + vlu * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < n - i - 1) then\n          j := i + 1 + j2 * 2\n          s := s - lu[i, n, j] * b[j]\n        end if\n        b[i] := s / lu[i, n, i]\n      end for\n    else\n      ! Multi-RHS: scalar path (b stride-nrhs in j)\n      for i := 1, n\n        s := b[i, nrhs, rhs]\n        for j := 0, i\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i, nrhs, rhs]\n        for j := i + 1, n\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s / lu[i, n, i]\n      end for\n    end if\n  end for\nend";

export const alpack_dgesv = "subroutine alpack.dgesv(\n  a: array f64; b: array f64;\n  n, nrhs: i32;\n  ipiv: array i32; info: array i32\n)\nbegin\n  call alpack.dgetrf(a, n, ipiv, info)\n  if (info[0] /= 0) then\n    call return()\n  end if\n  call alpack.dgetrs(a, ipiv, b, n, nrhs)\nend";

export const alpack_dtrtri = "subroutine alpack.dtrtri(a: array f64; n: i32; info: array i32)\nvar i, j, k: i32; s: f64\nbegin\n  info[0] := 0\n  for j := 0, n\n    if (a[j, n, j] == 0.0) then\n      info[0] := j + 1\n      call return()\n    end if\n    a[j, n, j] := 1.0 / a[j, n, j]\n    for i := j + 1, n\n      s := 0.0\n      for k := j, i\n        s := s - a[i, n, k] * a[k, n, j]\n      end for\n      a[i, n, j] := s / a[i, n, i]\n    end for\n  end for\nend";

export const alpack_dsyev3 = "subroutine alpack.dsyev3(a: array f64; w: array f64)\nvar\n  a00, a01, a02, a11, a12, a22: f64\n  p1, q, p2, p, b00, b11, b22, det_b, half_det: f64\n  phi, r, e1, e2, e3: f64\n  m: f64\n  pi: f64\nbegin\n  pi := 3.14159265358979323846\n\n  a00 := a[0, 3, 0]\n  a01 := a[0, 3, 1]\n  a02 := a[0, 3, 2]\n  a11 := a[1, 3, 1]\n  a12 := a[1, 3, 2]\n  a22 := a[2, 3, 2]\n\n  ! trace / 3\n  m := (a00 + a11 + a22) / 3.0\n\n  ! sum of squares of off-diagonal elements\n  p1 := a01 * a01 + a02 * a02 + a12 * a12\n\n  if (p1 <= 1e-30) then\n    ! diagonal matrix — eigenvalues are the diagonal entries, sort descending\n    e1 := a00\n    e2 := a11\n    e3 := a22\n    ! sort 3 values descending (bubble sort)\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    if (e3 > e2) then\n      r := e2\n      e2 := e3\n      e3 := r\n    end if\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n    call return()\n  end if\n\n  q := m\n  p2 := (a00 - q) * (a00 - q) + (a11 - q) * (a11 - q) + (a22 - q) * (a22 - q) + 2.0 * p1\n  p := sqrt(p2 / 6.0)\n\n  ! B = (A - q*I) / p  — only need determinant\n  b00 := (a00 - q) / p\n  b11 := (a11 - q) / p\n  b22 := (a22 - q) / p\n  ! det(B) for symmetric 3x3\n  det_b := b00 * (b11 * b22 - (a12 / p) * (a12 / p))\n         - (a01 / p) * ((a01 / p) * b22 - (a12 / p) * (a02 / p))\n         + (a02 / p) * ((a01 / p) * (a12 / p) - b11 * (a02 / p))\n  half_det := det_b / 2.0\n\n  ! clamp to [-1, 1] for acos\n  if (half_det <= -1.0) then\n    phi := pi / 3.0\n  else if (half_det >= 1.0) then\n    phi := 0.0\n  else\n    phi := atan2(sqrt(1.0 - half_det * half_det), half_det) / 3.0\n  end if\n\n  ! eigenvalues in descending order\n  e1 := q + 2.0 * p * cos(phi)\n  e3 := q + 2.0 * p * cos(phi + 2.0 * pi / 3.0)\n  e2 := 3.0 * q - e1 - e3\n\n  w[0] := e1\n  w[1] := e2\n  w[2] := e3\nend";

export const alpack_dsyev3v = "subroutine alpack.dsyev3v(a: array f64; w: array f64; v: array f64)\nvar\n  a00, a01, a02, a11, a12, a22: f64\n  p1, q, p2, p, b00, b11, b22, det_b, half_det: f64\n  phi, r, e1, e2, e3: f64\n  m: f64\n  pi: f64\n  ! eigenvector computation\n  k, best: i32\n  r0x, r0y, r0z, r1x, r1y, r1z, r2x, r2y, r2z: f64\n  cx, cy, cz: f64\n  n01, n12, n02, nmax, nrm: f64\nbegin\n  pi := 3.14159265358979323846\n\n  a00 := a[0, 3, 0]\n  a01 := a[0, 3, 1]\n  a02 := a[0, 3, 2]\n  a11 := a[1, 3, 1]\n  a12 := a[1, 3, 2]\n  a22 := a[2, 3, 2]\n\n  ! ── compute eigenvalues (same as dsyev3) ──\n  m := (a00 + a11 + a22) / 3.0\n  p1 := a01 * a01 + a02 * a02 + a12 * a12\n\n  if (p1 <= 1e-30) then\n    ! diagonal matrix\n    e1 := a00\n    e2 := a11\n    e3 := a22\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    if (e3 > e2) then\n      r := e2\n      e2 := e3\n      e3 := r\n    end if\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n    ! eigenvectors for diagonal: identity (re-ordered to match eigenvalue order)\n    ! Find which diagonal entry maps to which eigenvalue\n    ! For simplicity with the sorted eigenvalues, compute eigenvectors below\n  else\n    q := m\n    p2 := (a00 - q) * (a00 - q) + (a11 - q) * (a11 - q) + (a22 - q) * (a22 - q) + 2.0 * p1\n    p := sqrt(p2 / 6.0)\n\n    b00 := (a00 - q) / p\n    b11 := (a11 - q) / p\n    b22 := (a22 - q) / p\n    det_b := b00 * (b11 * b22 - (a12 / p) * (a12 / p))\n           - (a01 / p) * ((a01 / p) * b22 - (a12 / p) * (a02 / p))\n           + (a02 / p) * ((a01 / p) * (a12 / p) - b11 * (a02 / p))\n    half_det := det_b / 2.0\n\n    if (half_det <= -1.0) then\n      phi := pi / 3.0\n    else if (half_det >= 1.0) then\n      phi := 0.0\n    else\n      phi := atan2(sqrt(1.0 - half_det * half_det), half_det) / 3.0\n    end if\n\n    e1 := q + 2.0 * p * cos(phi)\n    e3 := q + 2.0 * p * cos(phi + 2.0 * pi / 3.0)\n    e2 := 3.0 * q - e1 - e3\n\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n  end if\n\n  ! ── compute eigenvectors via cross-product null-space ──\n  ! For each eigenvalue w[k], form B = A - w[k]*I, take cross product\n  ! of two rows with largest cross-product norm\n  for k := 0, 3\n    r0x := a00 - w[k]\n    r0y := a01\n    r0z := a02\n    r1x := a01\n    r1y := a11 - w[k]\n    r1z := a12\n    r2x := a02\n    r2y := a12\n    r2z := a22 - w[k]\n\n    ! cross products of row pairs\n    ! r0 x r1\n    cx := r0y * r1z - r0z * r1y\n    cy := r0z * r1x - r0x * r1z\n    cz := r0x * r1y - r0y * r1x\n    n01 := cx * cx + cy * cy + cz * cz\n\n    nmax := n01\n    ! store best cross product\n    v[0, 3, k] := cx\n    v[1, 3, k] := cy\n    v[2, 3, k] := cz\n\n    ! r1 x r2\n    cx := r1y * r2z - r1z * r2y\n    cy := r1z * r2x - r1x * r2z\n    cz := r1x * r2y - r1y * r2x\n    n12 := cx * cx + cy * cy + cz * cz\n    if (n12 > nmax) then\n      nmax := n12\n      v[0, 3, k] := cx\n      v[1, 3, k] := cy\n      v[2, 3, k] := cz\n    end if\n\n    ! r0 x r2\n    cx := r0y * r2z - r0z * r2y\n    cy := r0z * r2x - r0x * r2z\n    cz := r0x * r2y - r0y * r2x\n    n02 := cx * cx + cy * cy + cz * cz\n    if (n02 > nmax) then\n      nmax := n02\n      v[0, 3, k] := cx\n      v[1, 3, k] := cy\n      v[2, 3, k] := cz\n    end if\n\n    ! normalize\n    nrm := sqrt(nmax)\n    if (nrm > 1e-30) then\n      v[0, 3, k] := v[0, 3, k] / nrm\n      v[1, 3, k] := v[1, 3, k] / nrm\n      v[2, 3, k] := v[2, 3, k] / nrm\n    else\n      ! degenerate: use canonical basis vector\n      v[0, 3, k] := 0.0\n      v[1, 3, k] := 0.0\n      v[2, 3, k] := 0.0\n      v[k, 3, k] := 1.0\n    end if\n  end for\nend";

export const alpack_dsyev = "subroutine alpack.dsyev(\n  a: array f64; w: array f64;\n  n: i32; info: array i32\n)\nvar\n  i, j, p, q, iter, maxIter: i32\n  off, thresh, app, aqq, apq: f64\n  tau, t, c, s, tmp: f64\nbegin\n  info[0] := 0\n  maxIter := 100\n\n  ! initialize w to diagonal of a\n  for i := 0, n\n    w[i] := a[i, n, i]\n  end for\n\n  for iter := 0, maxIter\n    ! compute off-diagonal norm\n    off := 0.0\n    for p := 0, n\n      for q := p + 1, n\n        off := off + a[p, n, q] * a[p, n, q]\n      end for\n    end for\n    if (off < 1e-28) then\n      call return()\n    end if\n\n    thresh := if (iter < 4) then 0.2 * off / f64(n * n) else 0.0\n\n    for p := 0, n\n      for q := p + 1, n\n        apq := a[p, n, q]\n        if (abs(apq) <= thresh) then\n          ! skip small elements in early sweeps\n        else\n          app := w[p]\n          aqq := w[q]\n          ! Jacobi rotation angle\n          tau := (aqq - app) / (2.0 * apq)\n          if (tau >= 0.0) then\n            t := 1.0 / (tau + sqrt(1.0 + tau * tau))\n          else\n            t := 0.0 - 1.0 / (0.0 - tau + sqrt(1.0 + tau * tau))\n          end if\n          c := 1.0 / sqrt(1.0 + t * t)\n          s := t * c\n\n          ! update eigenvalues\n          w[p] := app - t * apq\n          w[q] := aqq + t * apq\n          a[p, n, q] := 0.0\n\n          ! rotate rows/columns p and q\n          for i := 0, p\n            tmp := a[i, n, p]\n            a[i, n, p] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := p + 1, q\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := q + 1, n\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[q, n, i]\n            a[q, n, i] := s * tmp + c * a[q, n, i]\n          end for\n        end if\n      end for\n    end for\n  end for\n  info[0] := 1\nend";

export const alpack_dsyevv = "subroutine alpack.dsyevv(\n  a: array f64; w: array f64; v: array f64;\n  n: i32; info: array i32\n)\nvar\n  i, j, p, q, iter, maxIter: i32\n  off, thresh, app, aqq, apq: f64\n  tau, t, c, s, tmp: f64\nbegin\n  info[0] := 0\n  maxIter := 100\n\n  ! initialize V = I\n  for i := 0, n\n    for j := 0, n\n      if (i == j) then\n        v[i, n, j] := 1.0\n      else\n        v[i, n, j] := 0.0\n      end if\n    end for\n  end for\n\n  ! initialize w to diagonal of a\n  for i := 0, n\n    w[i] := a[i, n, i]\n  end for\n\n  for iter := 0, maxIter\n    ! compute off-diagonal norm\n    off := 0.0\n    for p := 0, n\n      for q := p + 1, n\n        off := off + a[p, n, q] * a[p, n, q]\n      end for\n    end for\n    if (off < 1e-28) then\n      call return()\n    end if\n\n    thresh := if (iter < 4) then 0.2 * off / f64(n * n) else 0.0\n\n    for p := 0, n\n      for q := p + 1, n\n        apq := a[p, n, q]\n        if (abs(apq) <= thresh) then\n          ! skip small elements in early sweeps\n        else\n          app := w[p]\n          aqq := w[q]\n          tau := (aqq - app) / (2.0 * apq)\n          if (tau >= 0.0) then\n            t := 1.0 / (tau + sqrt(1.0 + tau * tau))\n          else\n            t := 0.0 - 1.0 / (0.0 - tau + sqrt(1.0 + tau * tau))\n          end if\n          c := 1.0 / sqrt(1.0 + t * t)\n          s := t * c\n\n          w[p] := app - t * apq\n          w[q] := aqq + t * apq\n          a[p, n, q] := 0.0\n\n          ! rotate rows/columns p and q\n          for i := 0, p\n            tmp := a[i, n, p]\n            a[i, n, p] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := p + 1, q\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := q + 1, n\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[q, n, i]\n            a[q, n, i] := s * tmp + c * a[q, n, i]\n          end for\n\n          ! accumulate into eigenvector matrix V\n          for i := 0, n\n            tmp := v[i, n, p]\n            v[i, n, p] := c * tmp - s * v[i, n, q]\n            v[i, n, q] := s * tmp + c * v[i, n, q]\n          end for\n        end if\n      end for\n    end for\n  end for\n  info[0] := 1\nend";

export const alas_sscal = "subroutine alas.sscal(x: array f32; n: i32; alpha: f32)\nvar\n  i, n4: i32\n  va, vx: f32x4\nbegin\n  va := f32x4.splat(alpha)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vx := va * vx\n    call v128.store(x, i, vx)\n  end for\n  for i := n4 * 4, n\n    x[i] := alpha * x[i]\n  end for\nend";

export const alas_scopy = "subroutine alas.scopy(x: array f32; y: array f32; n: i32)\nvar\n  i, n4: i32\n  vx: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    call v128.store(y, i, vx)\n  end for\n  for i := n4 * 4, n\n    y[i] := x[i]\n  end for\nend";

export const alas_saxpy = "subroutine alas.saxpy(x: array f32; y: array f32; n: i32; alpha: f32)\nvar\n  i, n4: i32\n  va, vx, vy: f32x4\nbegin\n  va := f32x4.splat(alpha)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vy := f32x4.relaxed_madd(va, vx, vy)\n    call v128.store(y, i, vy)\n  end for\n  for i := n4 * 4, n\n    y[i] := alpha * x[i] + y[i]\n  end for\nend";

export const alas_sdot = "function alas.sdot(x: array f32; y: array f32; n: i32): f32\nvar\n  i, n4: i32\n  vx, vy, vs: f32x4\nbegin\n  vs := f32x4.splat(0.0)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vs := vs + vx * vy\n  end for\n  alas.sdot := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n  for i := n4 * 4, n\n    alas.sdot := alas.sdot + x[i] * y[i]\n  end for\nend";

export const alas_snrm2 = "function alas.snrm2(x: array f32; n: i32): f32\nvar\n  i, n4: i32\n  vx, vs: f32x4\nbegin\n  vs := f32x4.splat(0.0)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vs := vs + vx * vx\n  end for\n  alas.snrm2 := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n  for i := n4 * 4, n\n    alas.snrm2 := alas.snrm2 + x[i] * x[i]\n  end for\n  alas.snrm2 := sqrt(alas.snrm2)\nend";

export const alas_sgemv = "subroutine alas.sgemv(\n  a: array f32; x: array f32; y: array f32;\n  m, n: i32; alpha, beta: f32\n)\nvar\n  i, j, n4, a_row, joff: i32\n  s: f32\n  vs, va, vx: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, m\n    vs := f32x4.splat(0.0)\n    a_row := i * n * 4\n    for j := 0, n4\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vx := v128.load(x, j)\n      vs := vs + va * vx\n    end for\n    s := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n    for j := n4 * 4, n\n      s := s + a[i, n, j] * x[j]\n    end for\n    y[i] := alpha * s + beta * y[i]\n  end for\nend";

export const alas_sger = "subroutine alas.sger(\n  a: array f32; x: array f32; y: array f32;\n  m, n: i32; alpha: f32\n)\nvar\n  i, j, n4, a_row, joff: i32\n  t: f32\n  tv, vy, va: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, m\n    t := alpha * x[i]\n    tv := f32x4.splat(t)\n    a_row := i * n * 4\n    for j := 0, n4\n      joff := j * 16\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f32x4.relaxed_madd(tv, vy, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    for j := n4 * 4, n\n      a[i, n, j] := a[i, n, j] + t * y[j]\n    end for\n  end for\nend";

export const alas_sgemm = "subroutine alas.sgemm(\n  a: array f32; b: array f32; c: array f32;\n  m, n, k: i32; alpha, beta: f32\n)\nvar\n  i, j, p, n4, c_row, b_row, joff: i32\n  t: f32\n  tv, vb, vc: f32x4\nbegin\n  if (beta == 0.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := 0.0\n      end for\n    end for\n  else if (beta /= 1.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := beta * c[i, n, j]\n      end for\n    end for\n  end if\n\n  n4 := n / 4\n  for i := 0, m\n    c_row := i * n * 4\n    for p := 0, k\n      t := alpha * a[i, k, p]\n      tv := f32x4.splat(t)\n      b_row := p * n * 4\n      for j := 0, n4\n        joff := j * 16\n        vb := v128.load_at(b, b_row + joff)\n        vc := v128.load_at(c, c_row + joff)\n        vc := f32x4.relaxed_madd(tv, vb, vc)\n        call v128.store_at(c, c_row + joff, vc)\n      end for\n      for j := n4 * 4, n\n        c[i, n, j] := c[i, n, j] + t * b[p, n, j]\n      end for\n    end for\n  end for\nend";

export const alpack_sgetrf = "subroutine alpack.sgetrf(\n  a: array f32; n: i32;\n  ipiv: array i32; info: array i32\n)\nvar\n  i, j, k, pivot, nrem, nrem4, ai_base, ak_base, joff: i32\n  t, inv, m, v: f32\n  mv, vk, vi: f32x4\nbegin\n  info[0] := 0\n  for k := 0, n\n    pivot := k\n    v := a[k, n, k]\n    t := abs(v)\n    for i := k + 1, n\n      v := a[i, n, k]\n      v := abs(v)\n      if (v > t) then\n        pivot := i\n        t := v\n      end if\n    end for\n    ipiv[k] := pivot\n\n    if (pivot /= k) then\n      for j := 0, n\n        t := a[k, n, j]\n        a[k, n, j] := a[pivot, n, j]\n        a[pivot, n, j] := t\n      end for\n    end if\n\n    if (a[k, n, k] == 0.0) then\n      info[0] := k + 1\n      call return()\n    end if\n\n    inv := 1.0 / a[k, n, k]\n    nrem := n - k - 1\n    nrem4 := nrem / 4\n    ak_base := (k * n + k + 1) * 4\n    for i := k + 1, n\n      m := a[i, n, k] * inv\n      a[i, n, k] := m\n      mv := f32x4.splat(m)\n      ai_base := (i * n + k + 1) * 4\n      for j := 0, nrem4\n        joff := j * 16\n        vk := v128.load_at(a, ak_base + joff)\n        vi := v128.load_at(a, ai_base + joff)\n        vi := f32x4.relaxed_nmadd(mv, vk, vi)\n        call v128.store_at(a, ai_base + joff, vi)\n      end for\n      for j := k + 1 + nrem4 * 4, n\n        a[i, n, j] := a[i, n, j] - m * a[k, n, j]\n      end for\n    end for\n  end for\nend";

export const alpack_sgetrs = "subroutine alpack.sgetrs(\n  lu: array f32; ipiv: array i32;\n  b: array f32; n, nrhs: i32\n)\nvar\n  rhs, i, j, p, j4, lu_row, joff: i32\n  s, t: f32\n  acc, vlu, vb: f32x4\nbegin\n  for rhs := 0, nrhs\n    for i := 0, n\n      p := ipiv[i]\n      if (p /= i) then\n        t := b[i, nrhs, rhs]\n        b[i, nrhs, rhs] := b[p, nrhs, rhs]\n        b[p, nrhs, rhs] := t\n      end if\n    end for\n\n    if (nrhs == 1) then\n      for i := 1, n\n        s := b[i]\n        j4 := i / 4\n        acc := f32x4.splat(0.0)\n        lu_row := i * n * 4\n        for j := 0, j4\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vlu * vb\n        end for\n        s := s - f32x4.extract_lane(acc, 0) - f32x4.extract_lane(acc, 1) - f32x4.extract_lane(acc, 2) - f32x4.extract_lane(acc, 3)\n        for j := j4 * 4, i\n          s := s - lu[i, n, j] * b[j]\n        end for\n        b[i] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i]\n        j4 := (n - i - 1) / 4\n        acc := f32x4.splat(0.0)\n        lu_row := (i * n + i + 1) * 4\n        for j := 0, j4\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load_at(b, (i + 1) * 4 + joff)\n          acc := acc + vlu * vb\n        end for\n        s := s - f32x4.extract_lane(acc, 0) - f32x4.extract_lane(acc, 1) - f32x4.extract_lane(acc, 2) - f32x4.extract_lane(acc, 3)\n        for j := i + 1 + j4 * 4, n\n          s := s - lu[i, n, j] * b[j]\n        end for\n        b[i] := s / lu[i, n, i]\n      end for\n    else\n      for i := 1, n\n        s := b[i, nrhs, rhs]\n        for j := 0, i\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i, nrhs, rhs]\n        for j := i + 1, n\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s / lu[i, n, i]\n      end for\n    end if\n  end for\nend";

export const alpack_sgesv = "subroutine alpack.sgesv(\n  a: array f32; b: array f32;\n  n, nrhs: i32;\n  ipiv: array i32; info: array i32\n)\nbegin\n  call alpack.sgetrf(a, n, ipiv, info)\n  if (info[0] /= 0) then\n    call return()\n  end if\n  call alpack.sgetrs(a, ipiv, b, n, nrhs)\nend";

export const sources = {
  "alas.dscal": "subroutine alas.dscal(x: array f64; n: i32; alpha: f64)\nvar\n  i, n2: i32\n  va, vx: f64x2\nbegin\n  va := f64x2.splat(alpha)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vx := va * vx\n    call v128.store(x, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    x[i] := alpha * x[i]\n  end if\nend",
  "alas.dcopy": "subroutine alas.dcopy(x: array f64; y: array f64; n: i32)\nvar\n  i, n2: i32\n  vx: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    call v128.store(y, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    y[i] := x[i]\n  end if\nend",
  "alas.daxpy": "subroutine alas.daxpy(x: array f64; y: array f64; n: i32; alpha: f64)\nvar\n  i, n2: i32\n  va, vx, vy: f64x2\nbegin\n  ! scalar version (for reference):\n  !   for i := 0, n\n  !     y[i] := alpha * x[i] + y[i]\n  !   end for\n  va := f64x2.splat(alpha)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vy := f64x2.relaxed_madd(va, vx, vy)\n    call v128.store(y, i, vy)\n  end for\n  ! scalar tail for odd element\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    y[i] := alpha * x[i] + y[i]\n  end if\nend",
  "alas.ddot": "function alas.ddot(x: array f64; y: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vy, vs: f64x2\nbegin\n  ! scalar version:\n  !   alas.ddot := 0.0\n  !   for i := 0, n\n  !     alas.ddot := alas.ddot + x[i] * y[i]\n  !   end for\n  ! NOTE: this loop uses separate mul + add rather than f64x2.relaxed_madd.\n  ! The reduction has a single accumulator vs that's recurrence-dependent\n  ! (vs[k] = vs[k-1] + ...); FMA serializes this dependency chain at FMA\n  ! latency, while separate mul+add lets the CPU overlap multiplication\n  ! with the previous iteration's add. Empirically ~30% faster than FMA\n  ! at 10K-100K on AMD Ryzen AI 9 HX 370. dgemm/dger/dgemv use FMA because\n  ! their inner loops have m×n parallel accumulators (no serialization).\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vs := vs + vx * vy\n  end for\n  alas.ddot := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  ! scalar tail\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.ddot := alas.ddot + x[i] * y[i]\n  end if\nend",
  "alas.dnrm2": "function alas.dnrm2(x: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vs: f64x2\nbegin\n  ! Same single-accumulator caveat as ddot — see note there.\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vs := vs + vx * vx\n  end for\n  alas.dnrm2 := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.dnrm2 := alas.dnrm2 + x[i] * x[i]\n  end if\n  alas.dnrm2 := sqrt(alas.dnrm2)\nend",
  "alas.dswap": "subroutine alas.dswap(x: array f64; y: array f64; n: i32)\nvar\n  i, n2: i32\n  t: f64\n  vx, vy: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    call v128.store(x, i, vy)\n    call v128.store(y, i, vx)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    t := x[i]\n    x[i] := y[i]\n    y[i] := t\n  end if\nend",
  "alas.dasum": "function alas.dasum(x: array f64; n: i32): f64\nvar\n  i, n2: i32\n  vx, vs: f64x2\nbegin\n  vs := f64x2.splat(0.0)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vs := vs + f64x2.abs(vx)\n  end for\n  alas.dasum := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    alas.dasum := alas.dasum + abs(x[i])\n  end if\nend",
  "alas.drot": "subroutine alas.drot(x: array f64; y: array f64; n: i32; c, s: f64)\nvar\n  i, n2: i32\n  t: f64\n  vc, vs, vx, vy, vt, vy_new: f64x2\nbegin\n  vc := f64x2.splat(c)\n  vs := f64x2.splat(s)\n  n2 := n / 2\n  for i := 0, n2\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vt := vc * vx + vs * vy\n    vy_new := vc * vy - vs * vx\n    call v128.store(y, i, vy_new)\n    call v128.store(x, i, vt)\n  end for\n  if (n2 * 2 < n) then\n    i := n2 * 2\n    t := c * x[i] + s * y[i]\n    y[i] := c * y[i] - s * x[i]\n    x[i] := t\n  end if\nend",
  "alas.drotg": "function alas.drotg(a, b: f64; cs: array f64): f64\nvar r, z: f64\nbegin\n  r := sqrt(a * a + b * b)\n  if (r == 0.0) then\n    cs[0] := 1.0\n    cs[1] := 0.0\n    alas.drotg := 0.0\n  else\n    cs[0] := a / r\n    cs[1] := b / r\n    alas.drotg := r\n  end if\nend",
  "alas.idamax": "function alas.idamax(x: array f64; n: i32): i32\nvar i: i32; mx: f64; v: f64\nbegin\n  alas.idamax := 0\n  mx := abs(x[0])\n  for i := 1, n\n    v := abs(x[i])\n    if (v > mx) then\n      mx := v\n      alas.idamax := i\n    end if\n  end for\nend",
  "alas.dgemv": "subroutine alas.dgemv(\n  a: array f64; x: array f64; y: array f64;\n  m, n: i32; alpha, beta: f64\n)\nvar\n  i, j, n2, a_row, joff: i32\n  s: f64\n  vs, va, vx: f64x2\nbegin\n  ! Single-accumulator reduction per row → mul+add not FMA (same rationale\n  ! as ddot — FMA serializes the in-row recurrence at FMA latency).\n  n2 := n / 2\n  for i := 0, m\n    vs := f64x2.splat(0.0)\n    a_row := i * n * 8\n    for j := 0, n2\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vx := v128.load(x, j)\n      vs := vs + va * vx\n    end for\n    s := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n    if (n2 * 2 < n) then\n      j := n2 * 2\n      s := s + a[i, n, j] * x[j]\n    end if\n    y[i] := alpha * s + beta * y[i]\n  end for\nend",
  "alas.dtrsv": "subroutine alas.dtrsv(\n  a: array f64; x: array f64;\n  n, uplo, trans: i32\n)\nvar\n  i, j, j2, a_row, joff: i32\n  s: f64\n  acc, va, vx: f64x2\nbegin\n  if (uplo == 0 and trans == 0) then\n    ! lower, no transpose: forward substitution. Inner reads stride-1 → SIMD.\n    for i := 0, n\n      s := x[i]\n      j2 := i / 2\n      acc := f64x2.splat(0.0)\n      a_row := i * n * 8\n      for j := 0, j2\n        joff := j * 16\n        va := v128.load_at(a, a_row + joff)\n        vx := v128.load(x, j)\n        acc := acc + va * vx\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < i) then\n        j := j2 * 2\n        s := s - a[i, n, j] * x[j]\n      end if\n      x[i] := s / a[i, n, i]\n    end for\n  else if (uplo == 0 and trans == 1) then\n    ! lower, transpose (= upper): back sub. Reads a[j,i] stride-n → scalar.\n    for i := n - 1, -1, -1\n      s := x[i]\n      for j := i + 1, n\n        s := s - a[j, n, i] * x[j]\n      end for\n      x[i] := s / a[i, n, i]\n    end for\n  else if (uplo == 1 and trans == 0) then\n    ! upper, no transpose: back substitution. Inner reads stride-1 → SIMD.\n    for i := n - 1, -1, -1\n      s := x[i]\n      j2 := (n - i - 1) / 2\n      acc := f64x2.splat(0.0)\n      a_row := (i * n + i + 1) * 8\n      for j := 0, j2\n        joff := j * 16\n        va := v128.load_at(a, a_row + joff)\n        vx := v128.load_at(x, (i + 1) * 8 + joff)\n        acc := acc + va * vx\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < n - i - 1) then\n        j := i + 1 + j2 * 2\n        s := s - a[i, n, j] * x[j]\n      end if\n      x[i] := s / a[i, n, i]\n    end for\n  else\n    ! upper, transpose (= lower): forward sub. Reads a[j,i] stride-n → scalar.\n    for i := 0, n\n      s := x[i]\n      for j := 0, i\n        s := s - a[j, n, i] * x[j]\n      end for\n      x[i] := s / a[i, n, i]\n    end for\n  end if\nend",
  "alas.dger": "subroutine alas.dger(\n  a: array f64; x: array f64; y: array f64;\n  m, n: i32; alpha: f64\n)\nvar\n  i, j, n2, a_row, joff: i32\n  t: f64\n  tv, vy, va: f64x2\nbegin\n  n2 := n / 2\n  for i := 0, m\n    t := alpha * x[i]\n    tv := f64x2.splat(t)\n    a_row := i * n * 8\n    for j := 0, n2\n      joff := j * 16\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f64x2.relaxed_madd(tv, vy, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (n2 * 2 < n) then\n      j := n2 * 2\n      a[i, n, j] := a[i, n, j] + t * y[j]\n    end if\n  end for\nend",
  "alas.dsymv": "subroutine alas.dsymv(\n  a: array f64; x: array f64; y: array f64;\n  n: i32; alpha, beta: f64\n)\nvar\n  i, j, j2, a_row, joff: i32\n  s, t: f64\n  vs, va, vat, vx, vy, vxi, valpha: f64x2\nbegin\n  ! scale y by beta\n  for i := 0, n\n    y[i] := beta * y[i]\n  end for\n  ! accumulate: diagonal + off-diagonal (symmetric)\n  ! Each j-iteration reads a[i,j], updates s (reduction) AND y[j] (scatter).\n  ! Both use the same scaled value alpha*a[i,j]. SIMD over j.\n  valpha := f64x2.splat(alpha)\n  for i := 0, n\n    vs := f64x2.splat(0.0)\n    vxi := f64x2.splat(x[i])\n    a_row := i * n * 8\n    j2 := i / 2\n    for j := 0, j2\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vat := valpha * va                      ! alpha * a[i, j..j+1]\n      vx := v128.load(x, j)\n      vs := vs + vat * vx                     ! s += alpha*a[i,j] * x[j]\n      vy := v128.load(y, j)\n      vy := vy + vat * vxi                    ! y[j] += alpha*a[i,j] * x[i]\n      call v128.store(y, j, vy)\n    end for\n    s := f64x2.extract_lane(vs, 0) + f64x2.extract_lane(vs, 1)\n    if (j2 * 2 < i) then\n      j := j2 * 2\n      t := alpha * a[i, n, j]\n      s := s + t * x[j]\n      y[j] := y[j] + t * x[i]\n    end if\n    y[i] := y[i] + s + alpha * a[i, n, i] * x[i]\n  end for\nend",
  "alas.dsyr": "subroutine alas.dsyr(\n  a: array f64; x: array f64;\n  n: i32; alpha: f64\n)\nvar\n  i, j, ip1, j2, a_row, joff: i32\n  t: f64\n  tv, vx, va: f64x2\nbegin\n  for i := 0, n\n    t := alpha * x[i]\n    tv := f64x2.splat(t)\n    a_row := i * n * 8\n    ip1 := i + 1\n    j2 := ip1 / 2\n    for j := 0, j2\n      joff := j * 16\n      vx := v128.load(x, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f64x2.relaxed_madd(tv, vx, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (j2 * 2 < ip1) then\n      j := j2 * 2\n      a[i, n, j] := a[i, n, j] + t * x[j]\n    end if\n  end for\nend",
  "alas.dsyr2": "subroutine alas.dsyr2(\n  a: array f64; x: array f64; y: array f64;\n  n: i32; alpha: f64\n)\nvar\n  i, j, ip1, j2, a_row, joff: i32\n  tx, ty: f64\n  tvx, tvy, vx, vy, va: f64x2\nbegin\n  for i := 0, n\n    tx := alpha * x[i]\n    ty := alpha * y[i]\n    tvx := f64x2.splat(tx)\n    tvy := f64x2.splat(ty)\n    a_row := i * n * 8\n    ip1 := i + 1\n    j2 := ip1 / 2\n    for j := 0, j2\n      joff := j * 16\n      vx := v128.load(x, j)\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      ! a[i,j] += tx * y[j] + ty * x[j]  via two FMAs\n      va := f64x2.relaxed_madd(tvx, vy, va)\n      va := f64x2.relaxed_madd(tvy, vx, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    if (j2 * 2 < ip1) then\n      j := j2 * 2\n      a[i, n, j] := a[i, n, j] + tx * y[j] + ty * x[j]\n    end if\n  end for\nend",
  "alas.dtrmv": "subroutine alas.dtrmv(\n  a: array f64; x: array f64;\n  n, uplo, trans, diag: i32\n)\nvar i, j: i32; s: f64\nbegin\n  if (uplo == 1 and trans == 0) then\n    ! upper, no transpose: forward\n    for i := 0, n\n      s := 0.0\n      if (diag == 1) then\n        s := x[i]\n      else\n        s := a[i, n, i] * x[i]\n      end if\n      for j := i + 1, n\n        s := s + a[i, n, j] * x[j]\n      end for\n      x[i] := s\n    end for\n  else if (uplo == 0 and trans == 0) then\n    ! lower, no transpose: backward\n    for i := n - 1, -1, -1\n      s := 0.0\n      for j := 0, i\n        s := s + a[i, n, j] * x[j]\n      end for\n      if (diag == 1) then\n        s := s + x[i]\n      else\n        s := s + a[i, n, i] * x[i]\n      end if\n      x[i] := s\n    end for\n  else if (uplo == 1 and trans == 1) then\n    ! upper, transpose (= lower): backward\n    for i := n - 1, -1, -1\n      s := 0.0\n      for j := 0, i\n        s := s + a[j, n, i] * x[j]\n      end for\n      if (diag == 1) then\n        s := s + x[i]\n      else\n        s := s + a[i, n, i] * x[i]\n      end if\n      x[i] := s\n    end for\n  else\n    ! lower, transpose (= upper): forward\n    for i := 0, n\n      s := 0.0\n      if (diag == 1) then\n        s := x[i]\n      else\n        s := a[i, n, i] * x[i]\n      end if\n      for j := i + 1, n\n        s := s + a[j, n, i] * x[j]\n      end for\n      x[i] := s\n    end for\n  end if\nend",
  "alas.dgemm": "subroutine alas.dgemm(\n  a: array f64; b: array f64; c: array f64;\n  m, n, k: i32; alpha, beta: f64\n)\nvar\n  i, j, p, n2, c_row, b_row, joff: i32\n  t: f64\n  tv, vb, vc: f64x2\nbegin\n  ! Phase 1: prepare C.\n  if (beta == 0.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := 0.0\n      end for\n    end for\n  else if (beta /= 1.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := beta * c[i, n, j]\n      end for\n    end for\n  end if\n\n  ! Phase 2: C += alpha * A * B with SIMD inner loop.\n  n2 := n / 2\n  for i := 0, m\n    c_row := i * n * 8         ! byte offset of row i in C\n    for p := 0, k\n      t := alpha * a[i, k, p]\n      tv := f64x2.splat(t)\n      b_row := p * n * 8       ! byte offset of row p in B\n      for j := 0, n2\n        joff := j * 16\n        vb := v128.load_at(b, b_row + joff)\n        vc := v128.load_at(c, c_row + joff)\n        vc := f64x2.relaxed_madd(tv, vb, vc)\n        call v128.store_at(c, c_row + joff, vc)\n      end for\n      ! scalar tail for odd n\n      if (n2 * 2 < n) then\n        j := n2 * 2\n        c[i, n, j] := c[i, n, j] + t * b[p, n, j]\n      end if\n    end for\n  end for\nend",
  "alpack.dpotrf": "subroutine alpack.dpotrf(a: array f64; n: i32; info: array i32)\nvar\n  i, j, k, j2, ai_base, aj_base, koff: i32\n  s: f64\n  acc, va, vj: f64x2\nbegin\n  info[0] := 0\n  for j := 0, n\n    ! diagonal: s := a[j,j] - sum_{k<j} a[j,k]^2\n    s := a[j, n, j]\n    j2 := j / 2\n    acc := f64x2.splat(0.0)\n    aj_base := j * n * 8\n    for k := 0, j2\n      koff := k * 16\n      vj := v128.load_at(a, aj_base + koff)\n      acc := acc + vj * vj\n    end for\n    s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n    if (j2 * 2 < j) then\n      k := j2 * 2\n      s := s - a[j, n, k] * a[j, n, k]\n    end if\n\n    if (s <= 0.0) then\n      info[0] := j + 1\n      call return()\n    end if\n    a[j, n, j] := sqrt(s)\n\n    ! off-diagonal: s := a[i,j] - sum_{k<j} a[i,k] * a[j,k]\n    for i := j + 1, n\n      s := a[i, n, j]\n      acc := f64x2.splat(0.0)\n      ai_base := i * n * 8\n      for k := 0, j2\n        koff := k * 16\n        va := v128.load_at(a, ai_base + koff)\n        vj := v128.load_at(a, aj_base + koff)\n        acc := acc + va * vj\n      end for\n      s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n      if (j2 * 2 < j) then\n        k := j2 * 2\n        s := s - a[i, n, k] * a[j, n, k]\n      end if\n      a[i, n, j] := s / a[j, n, j]\n    end for\n  end for\nend",
  "alpack.dpotrs": "subroutine alpack.dpotrs(\n  l: array f64; b: array f64;\n  n, nrhs: i32\n)\nvar\n  rhs, i, j, j2, l_row, joff: i32\n  s: f64\n  acc, vl, vb: f64x2\nbegin\n  for rhs := 0, nrhs\n    if (nrhs == 1) then\n      ! forward substitution (SIMD nrhs=1 path)\n      for i := 0, n\n        s := b[i]\n        j2 := i / 2\n        acc := f64x2.splat(0.0)\n        l_row := i * n * 8\n        for j := 0, j2\n          joff := j * 16\n          vl := v128.load_at(l, l_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vl * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < i) then\n          j := j2 * 2\n          s := s - l[i, n, j] * b[j]\n        end if\n        b[i] := s / l[i, n, i]\n      end for\n      ! back substitution: column-major access on L → scalar\n      for i := n - 1, -1, -1\n        s := b[i]\n        for j := i + 1, n\n          s := s - l[j, n, i] * b[j]\n        end for\n        b[i] := s / l[i, n, i]\n      end for\n    else\n    ! Multi-RHS scalar path\n    ! forward substitution: L * y = b\n    for i := 0, n\n      s := b[i, nrhs, rhs]\n      for j := 0, i\n        s := s - l[i, n, j] * b[j, nrhs, rhs]\n      end for\n      b[i, nrhs, rhs] := s / l[i, n, i]\n    end for\n    ! back substitution: L^T * x = y\n    for i := n - 1, -1, -1\n      s := b[i, nrhs, rhs]\n      for j := i + 1, n\n        s := s - l[j, n, i] * b[j, nrhs, rhs]\n      end for\n      b[i, nrhs, rhs] := s / l[i, n, i]\n    end for\n    end if\n  end for\nend",
  "alpack.dgetrf": "subroutine alpack.dgetrf(\n  a: array f64; n: i32;\n  ipiv: array i32; info: array i32\n)\nvar\n  i, j, k, pivot, nrem, nrem2, ai_base, ak_base, joff: i32\n  t, inv, m: f64\n  mv, vk, vi: f64x2\nbegin\n  info[0] := 0\n  for k := 0, n\n    ! find pivot in column k from row k..n-1\n    pivot := k\n    t := abs(a[k, n, k])\n    for i := k + 1, n\n      if (abs(a[i, n, k]) > t) then\n        pivot := i\n        t := abs(a[i, n, k])\n      end if\n    end for\n    ipiv[k] := pivot\n\n    ! swap rows k and pivot\n    if (pivot /= k) then\n      for j := 0, n\n        t := a[k, n, j]\n        a[k, n, j] := a[pivot, n, j]\n        a[pivot, n, j] := t\n      end for\n    end if\n\n    if (a[k, n, k] == 0.0) then\n      info[0] := k + 1\n      call return()\n    end if\n\n    ! eliminate below diagonal: rank-1 update of the trailing (n-k-1)×(n-k-1)\n    ! submatrix using the daxpy/nmadd pattern over the inner j loop.\n    inv := 1.0 / a[k, n, k]\n    nrem := n - k - 1\n    nrem2 := nrem / 2\n    ak_base := (k * n + k + 1) * 8\n    for i := k + 1, n\n      m := a[i, n, k] * inv\n      a[i, n, k] := m\n      mv := f64x2.splat(m)\n      ai_base := (i * n + k + 1) * 8\n      for j := 0, nrem2\n        joff := j * 16\n        vk := v128.load_at(a, ak_base + joff)\n        vi := v128.load_at(a, ai_base + joff)\n        vi := f64x2.relaxed_nmadd(mv, vk, vi)\n        call v128.store_at(a, ai_base + joff, vi)\n      end for\n      ! scalar tail when nrem is odd\n      if (nrem2 * 2 < nrem) then\n        j := k + 1 + nrem2 * 2\n        a[i, n, j] := a[i, n, j] - m * a[k, n, j]\n      end if\n    end for\n  end for\nend",
  "alpack.dgetrs": "subroutine alpack.dgetrs(\n  lu: array f64; ipiv: array i32;\n  b: array f64; n, nrhs: i32\n)\nvar\n  rhs, i, j, p, j2, lu_row, joff: i32\n  s, t: f64\n  acc, vlu, vb: f64x2\nbegin\n  for rhs := 0, nrhs\n    ! apply row permutation\n    for i := 0, n\n      p := ipiv[i]\n      if (p /= i) then\n        t := b[i, nrhs, rhs]\n        b[i, nrhs, rhs] := b[p, nrhs, rhs]\n        b[p, nrhs, rhs] := t\n      end if\n    end for\n\n    if (nrhs == 1) then\n      ! SIMD fast path — b stride-1 in j\n      ! forward substitution: s := b[i] - sum_{j<i} lu[i,j] * b[j]\n      for i := 1, n\n        s := b[i]\n        j2 := i / 2\n        acc := f64x2.splat(0.0)\n        lu_row := i * n * 8\n        for j := 0, j2\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vlu * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < i) then\n          j := j2 * 2\n          s := s - lu[i, n, j] * b[j]\n        end if\n        b[i] := s\n      end for\n      ! back substitution\n      for i := n - 1, -1, -1\n        s := b[i]\n        ! reduce over j in [i+1, n)\n        j2 := (n - i - 1) / 2\n        acc := f64x2.splat(0.0)\n        lu_row := (i * n + i + 1) * 8\n        for j := 0, j2\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load_at(b, (i + 1) * 8 + joff)\n          acc := acc + vlu * vb\n        end for\n        s := s - f64x2.extract_lane(acc, 0) - f64x2.extract_lane(acc, 1)\n        if (j2 * 2 < n - i - 1) then\n          j := i + 1 + j2 * 2\n          s := s - lu[i, n, j] * b[j]\n        end if\n        b[i] := s / lu[i, n, i]\n      end for\n    else\n      ! Multi-RHS: scalar path (b stride-nrhs in j)\n      for i := 1, n\n        s := b[i, nrhs, rhs]\n        for j := 0, i\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i, nrhs, rhs]\n        for j := i + 1, n\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s / lu[i, n, i]\n      end for\n    end if\n  end for\nend",
  "alpack.dgesv": "subroutine alpack.dgesv(\n  a: array f64; b: array f64;\n  n, nrhs: i32;\n  ipiv: array i32; info: array i32\n)\nbegin\n  call alpack.dgetrf(a, n, ipiv, info)\n  if (info[0] /= 0) then\n    call return()\n  end if\n  call alpack.dgetrs(a, ipiv, b, n, nrhs)\nend",
  "alpack.dtrtri": "subroutine alpack.dtrtri(a: array f64; n: i32; info: array i32)\nvar i, j, k: i32; s: f64\nbegin\n  info[0] := 0\n  for j := 0, n\n    if (a[j, n, j] == 0.0) then\n      info[0] := j + 1\n      call return()\n    end if\n    a[j, n, j] := 1.0 / a[j, n, j]\n    for i := j + 1, n\n      s := 0.0\n      for k := j, i\n        s := s - a[i, n, k] * a[k, n, j]\n      end for\n      a[i, n, j] := s / a[i, n, i]\n    end for\n  end for\nend",
  "alpack.dsyev3": "subroutine alpack.dsyev3(a: array f64; w: array f64)\nvar\n  a00, a01, a02, a11, a12, a22: f64\n  p1, q, p2, p, b00, b11, b22, det_b, half_det: f64\n  phi, r, e1, e2, e3: f64\n  m: f64\n  pi: f64\nbegin\n  pi := 3.14159265358979323846\n\n  a00 := a[0, 3, 0]\n  a01 := a[0, 3, 1]\n  a02 := a[0, 3, 2]\n  a11 := a[1, 3, 1]\n  a12 := a[1, 3, 2]\n  a22 := a[2, 3, 2]\n\n  ! trace / 3\n  m := (a00 + a11 + a22) / 3.0\n\n  ! sum of squares of off-diagonal elements\n  p1 := a01 * a01 + a02 * a02 + a12 * a12\n\n  if (p1 <= 1e-30) then\n    ! diagonal matrix — eigenvalues are the diagonal entries, sort descending\n    e1 := a00\n    e2 := a11\n    e3 := a22\n    ! sort 3 values descending (bubble sort)\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    if (e3 > e2) then\n      r := e2\n      e2 := e3\n      e3 := r\n    end if\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n    call return()\n  end if\n\n  q := m\n  p2 := (a00 - q) * (a00 - q) + (a11 - q) * (a11 - q) + (a22 - q) * (a22 - q) + 2.0 * p1\n  p := sqrt(p2 / 6.0)\n\n  ! B = (A - q*I) / p  — only need determinant\n  b00 := (a00 - q) / p\n  b11 := (a11 - q) / p\n  b22 := (a22 - q) / p\n  ! det(B) for symmetric 3x3\n  det_b := b00 * (b11 * b22 - (a12 / p) * (a12 / p))\n         - (a01 / p) * ((a01 / p) * b22 - (a12 / p) * (a02 / p))\n         + (a02 / p) * ((a01 / p) * (a12 / p) - b11 * (a02 / p))\n  half_det := det_b / 2.0\n\n  ! clamp to [-1, 1] for acos\n  if (half_det <= -1.0) then\n    phi := pi / 3.0\n  else if (half_det >= 1.0) then\n    phi := 0.0\n  else\n    phi := atan2(sqrt(1.0 - half_det * half_det), half_det) / 3.0\n  end if\n\n  ! eigenvalues in descending order\n  e1 := q + 2.0 * p * cos(phi)\n  e3 := q + 2.0 * p * cos(phi + 2.0 * pi / 3.0)\n  e2 := 3.0 * q - e1 - e3\n\n  w[0] := e1\n  w[1] := e2\n  w[2] := e3\nend",
  "alpack.dsyev3v": "subroutine alpack.dsyev3v(a: array f64; w: array f64; v: array f64)\nvar\n  a00, a01, a02, a11, a12, a22: f64\n  p1, q, p2, p, b00, b11, b22, det_b, half_det: f64\n  phi, r, e1, e2, e3: f64\n  m: f64\n  pi: f64\n  ! eigenvector computation\n  k, best: i32\n  r0x, r0y, r0z, r1x, r1y, r1z, r2x, r2y, r2z: f64\n  cx, cy, cz: f64\n  n01, n12, n02, nmax, nrm: f64\nbegin\n  pi := 3.14159265358979323846\n\n  a00 := a[0, 3, 0]\n  a01 := a[0, 3, 1]\n  a02 := a[0, 3, 2]\n  a11 := a[1, 3, 1]\n  a12 := a[1, 3, 2]\n  a22 := a[2, 3, 2]\n\n  ! ── compute eigenvalues (same as dsyev3) ──\n  m := (a00 + a11 + a22) / 3.0\n  p1 := a01 * a01 + a02 * a02 + a12 * a12\n\n  if (p1 <= 1e-30) then\n    ! diagonal matrix\n    e1 := a00\n    e2 := a11\n    e3 := a22\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    if (e3 > e2) then\n      r := e2\n      e2 := e3\n      e3 := r\n    end if\n    if (e2 > e1) then\n      r := e1\n      e1 := e2\n      e2 := r\n    end if\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n    ! eigenvectors for diagonal: identity (re-ordered to match eigenvalue order)\n    ! Find which diagonal entry maps to which eigenvalue\n    ! For simplicity with the sorted eigenvalues, compute eigenvectors below\n  else\n    q := m\n    p2 := (a00 - q) * (a00 - q) + (a11 - q) * (a11 - q) + (a22 - q) * (a22 - q) + 2.0 * p1\n    p := sqrt(p2 / 6.0)\n\n    b00 := (a00 - q) / p\n    b11 := (a11 - q) / p\n    b22 := (a22 - q) / p\n    det_b := b00 * (b11 * b22 - (a12 / p) * (a12 / p))\n           - (a01 / p) * ((a01 / p) * b22 - (a12 / p) * (a02 / p))\n           + (a02 / p) * ((a01 / p) * (a12 / p) - b11 * (a02 / p))\n    half_det := det_b / 2.0\n\n    if (half_det <= -1.0) then\n      phi := pi / 3.0\n    else if (half_det >= 1.0) then\n      phi := 0.0\n    else\n      phi := atan2(sqrt(1.0 - half_det * half_det), half_det) / 3.0\n    end if\n\n    e1 := q + 2.0 * p * cos(phi)\n    e3 := q + 2.0 * p * cos(phi + 2.0 * pi / 3.0)\n    e2 := 3.0 * q - e1 - e3\n\n    w[0] := e1\n    w[1] := e2\n    w[2] := e3\n  end if\n\n  ! ── compute eigenvectors via cross-product null-space ──\n  ! For each eigenvalue w[k], form B = A - w[k]*I, take cross product\n  ! of two rows with largest cross-product norm\n  for k := 0, 3\n    r0x := a00 - w[k]\n    r0y := a01\n    r0z := a02\n    r1x := a01\n    r1y := a11 - w[k]\n    r1z := a12\n    r2x := a02\n    r2y := a12\n    r2z := a22 - w[k]\n\n    ! cross products of row pairs\n    ! r0 x r1\n    cx := r0y * r1z - r0z * r1y\n    cy := r0z * r1x - r0x * r1z\n    cz := r0x * r1y - r0y * r1x\n    n01 := cx * cx + cy * cy + cz * cz\n\n    nmax := n01\n    ! store best cross product\n    v[0, 3, k] := cx\n    v[1, 3, k] := cy\n    v[2, 3, k] := cz\n\n    ! r1 x r2\n    cx := r1y * r2z - r1z * r2y\n    cy := r1z * r2x - r1x * r2z\n    cz := r1x * r2y - r1y * r2x\n    n12 := cx * cx + cy * cy + cz * cz\n    if (n12 > nmax) then\n      nmax := n12\n      v[0, 3, k] := cx\n      v[1, 3, k] := cy\n      v[2, 3, k] := cz\n    end if\n\n    ! r0 x r2\n    cx := r0y * r2z - r0z * r2y\n    cy := r0z * r2x - r0x * r2z\n    cz := r0x * r2y - r0y * r2x\n    n02 := cx * cx + cy * cy + cz * cz\n    if (n02 > nmax) then\n      nmax := n02\n      v[0, 3, k] := cx\n      v[1, 3, k] := cy\n      v[2, 3, k] := cz\n    end if\n\n    ! normalize\n    nrm := sqrt(nmax)\n    if (nrm > 1e-30) then\n      v[0, 3, k] := v[0, 3, k] / nrm\n      v[1, 3, k] := v[1, 3, k] / nrm\n      v[2, 3, k] := v[2, 3, k] / nrm\n    else\n      ! degenerate: use canonical basis vector\n      v[0, 3, k] := 0.0\n      v[1, 3, k] := 0.0\n      v[2, 3, k] := 0.0\n      v[k, 3, k] := 1.0\n    end if\n  end for\nend",
  "alpack.dsyev": "subroutine alpack.dsyev(\n  a: array f64; w: array f64;\n  n: i32; info: array i32\n)\nvar\n  i, j, p, q, iter, maxIter: i32\n  off, thresh, app, aqq, apq: f64\n  tau, t, c, s, tmp: f64\nbegin\n  info[0] := 0\n  maxIter := 100\n\n  ! initialize w to diagonal of a\n  for i := 0, n\n    w[i] := a[i, n, i]\n  end for\n\n  for iter := 0, maxIter\n    ! compute off-diagonal norm\n    off := 0.0\n    for p := 0, n\n      for q := p + 1, n\n        off := off + a[p, n, q] * a[p, n, q]\n      end for\n    end for\n    if (off < 1e-28) then\n      call return()\n    end if\n\n    thresh := if (iter < 4) then 0.2 * off / f64(n * n) else 0.0\n\n    for p := 0, n\n      for q := p + 1, n\n        apq := a[p, n, q]\n        if (abs(apq) <= thresh) then\n          ! skip small elements in early sweeps\n        else\n          app := w[p]\n          aqq := w[q]\n          ! Jacobi rotation angle\n          tau := (aqq - app) / (2.0 * apq)\n          if (tau >= 0.0) then\n            t := 1.0 / (tau + sqrt(1.0 + tau * tau))\n          else\n            t := 0.0 - 1.0 / (0.0 - tau + sqrt(1.0 + tau * tau))\n          end if\n          c := 1.0 / sqrt(1.0 + t * t)\n          s := t * c\n\n          ! update eigenvalues\n          w[p] := app - t * apq\n          w[q] := aqq + t * apq\n          a[p, n, q] := 0.0\n\n          ! rotate rows/columns p and q\n          for i := 0, p\n            tmp := a[i, n, p]\n            a[i, n, p] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := p + 1, q\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := q + 1, n\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[q, n, i]\n            a[q, n, i] := s * tmp + c * a[q, n, i]\n          end for\n        end if\n      end for\n    end for\n  end for\n  info[0] := 1\nend",
  "alpack.dsyevv": "subroutine alpack.dsyevv(\n  a: array f64; w: array f64; v: array f64;\n  n: i32; info: array i32\n)\nvar\n  i, j, p, q, iter, maxIter: i32\n  off, thresh, app, aqq, apq: f64\n  tau, t, c, s, tmp: f64\nbegin\n  info[0] := 0\n  maxIter := 100\n\n  ! initialize V = I\n  for i := 0, n\n    for j := 0, n\n      if (i == j) then\n        v[i, n, j] := 1.0\n      else\n        v[i, n, j] := 0.0\n      end if\n    end for\n  end for\n\n  ! initialize w to diagonal of a\n  for i := 0, n\n    w[i] := a[i, n, i]\n  end for\n\n  for iter := 0, maxIter\n    ! compute off-diagonal norm\n    off := 0.0\n    for p := 0, n\n      for q := p + 1, n\n        off := off + a[p, n, q] * a[p, n, q]\n      end for\n    end for\n    if (off < 1e-28) then\n      call return()\n    end if\n\n    thresh := if (iter < 4) then 0.2 * off / f64(n * n) else 0.0\n\n    for p := 0, n\n      for q := p + 1, n\n        apq := a[p, n, q]\n        if (abs(apq) <= thresh) then\n          ! skip small elements in early sweeps\n        else\n          app := w[p]\n          aqq := w[q]\n          tau := (aqq - app) / (2.0 * apq)\n          if (tau >= 0.0) then\n            t := 1.0 / (tau + sqrt(1.0 + tau * tau))\n          else\n            t := 0.0 - 1.0 / (0.0 - tau + sqrt(1.0 + tau * tau))\n          end if\n          c := 1.0 / sqrt(1.0 + t * t)\n          s := t * c\n\n          w[p] := app - t * apq\n          w[q] := aqq + t * apq\n          a[p, n, q] := 0.0\n\n          ! rotate rows/columns p and q\n          for i := 0, p\n            tmp := a[i, n, p]\n            a[i, n, p] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := p + 1, q\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[i, n, q]\n            a[i, n, q] := s * tmp + c * a[i, n, q]\n          end for\n          for i := q + 1, n\n            tmp := a[p, n, i]\n            a[p, n, i] := c * tmp - s * a[q, n, i]\n            a[q, n, i] := s * tmp + c * a[q, n, i]\n          end for\n\n          ! accumulate into eigenvector matrix V\n          for i := 0, n\n            tmp := v[i, n, p]\n            v[i, n, p] := c * tmp - s * v[i, n, q]\n            v[i, n, q] := s * tmp + c * v[i, n, q]\n          end for\n        end if\n      end for\n    end for\n  end for\n  info[0] := 1\nend",
  "alas.sscal": "subroutine alas.sscal(x: array f32; n: i32; alpha: f32)\nvar\n  i, n4: i32\n  va, vx: f32x4\nbegin\n  va := f32x4.splat(alpha)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vx := va * vx\n    call v128.store(x, i, vx)\n  end for\n  for i := n4 * 4, n\n    x[i] := alpha * x[i]\n  end for\nend",
  "alas.scopy": "subroutine alas.scopy(x: array f32; y: array f32; n: i32)\nvar\n  i, n4: i32\n  vx: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    call v128.store(y, i, vx)\n  end for\n  for i := n4 * 4, n\n    y[i] := x[i]\n  end for\nend",
  "alas.saxpy": "subroutine alas.saxpy(x: array f32; y: array f32; n: i32; alpha: f32)\nvar\n  i, n4: i32\n  va, vx, vy: f32x4\nbegin\n  va := f32x4.splat(alpha)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vy := f32x4.relaxed_madd(va, vx, vy)\n    call v128.store(y, i, vy)\n  end for\n  for i := n4 * 4, n\n    y[i] := alpha * x[i] + y[i]\n  end for\nend",
  "alas.sdot": "function alas.sdot(x: array f32; y: array f32; n: i32): f32\nvar\n  i, n4: i32\n  vx, vy, vs: f32x4\nbegin\n  vs := f32x4.splat(0.0)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vy := v128.load(y, i)\n    vs := vs + vx * vy\n  end for\n  alas.sdot := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n  for i := n4 * 4, n\n    alas.sdot := alas.sdot + x[i] * y[i]\n  end for\nend",
  "alas.snrm2": "function alas.snrm2(x: array f32; n: i32): f32\nvar\n  i, n4: i32\n  vx, vs: f32x4\nbegin\n  vs := f32x4.splat(0.0)\n  n4 := n / 4\n  for i := 0, n4\n    vx := v128.load(x, i)\n    vs := vs + vx * vx\n  end for\n  alas.snrm2 := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n  for i := n4 * 4, n\n    alas.snrm2 := alas.snrm2 + x[i] * x[i]\n  end for\n  alas.snrm2 := sqrt(alas.snrm2)\nend",
  "alas.sgemv": "subroutine alas.sgemv(\n  a: array f32; x: array f32; y: array f32;\n  m, n: i32; alpha, beta: f32\n)\nvar\n  i, j, n4, a_row, joff: i32\n  s: f32\n  vs, va, vx: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, m\n    vs := f32x4.splat(0.0)\n    a_row := i * n * 4\n    for j := 0, n4\n      joff := j * 16\n      va := v128.load_at(a, a_row + joff)\n      vx := v128.load(x, j)\n      vs := vs + va * vx\n    end for\n    s := f32x4.extract_lane(vs, 0) + f32x4.extract_lane(vs, 1) + f32x4.extract_lane(vs, 2) + f32x4.extract_lane(vs, 3)\n    for j := n4 * 4, n\n      s := s + a[i, n, j] * x[j]\n    end for\n    y[i] := alpha * s + beta * y[i]\n  end for\nend",
  "alas.sger": "subroutine alas.sger(\n  a: array f32; x: array f32; y: array f32;\n  m, n: i32; alpha: f32\n)\nvar\n  i, j, n4, a_row, joff: i32\n  t: f32\n  tv, vy, va: f32x4\nbegin\n  n4 := n / 4\n  for i := 0, m\n    t := alpha * x[i]\n    tv := f32x4.splat(t)\n    a_row := i * n * 4\n    for j := 0, n4\n      joff := j * 16\n      vy := v128.load(y, j)\n      va := v128.load_at(a, a_row + joff)\n      va := f32x4.relaxed_madd(tv, vy, va)\n      call v128.store_at(a, a_row + joff, va)\n    end for\n    for j := n4 * 4, n\n      a[i, n, j] := a[i, n, j] + t * y[j]\n    end for\n  end for\nend",
  "alas.sgemm": "subroutine alas.sgemm(\n  a: array f32; b: array f32; c: array f32;\n  m, n, k: i32; alpha, beta: f32\n)\nvar\n  i, j, p, n4, c_row, b_row, joff: i32\n  t: f32\n  tv, vb, vc: f32x4\nbegin\n  if (beta == 0.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := 0.0\n      end for\n    end for\n  else if (beta /= 1.0) then\n    for i := 0, m\n      for j := 0, n\n        c[i, n, j] := beta * c[i, n, j]\n      end for\n    end for\n  end if\n\n  n4 := n / 4\n  for i := 0, m\n    c_row := i * n * 4\n    for p := 0, k\n      t := alpha * a[i, k, p]\n      tv := f32x4.splat(t)\n      b_row := p * n * 4\n      for j := 0, n4\n        joff := j * 16\n        vb := v128.load_at(b, b_row + joff)\n        vc := v128.load_at(c, c_row + joff)\n        vc := f32x4.relaxed_madd(tv, vb, vc)\n        call v128.store_at(c, c_row + joff, vc)\n      end for\n      for j := n4 * 4, n\n        c[i, n, j] := c[i, n, j] + t * b[p, n, j]\n      end for\n    end for\n  end for\nend",
  "alpack.sgetrf": "subroutine alpack.sgetrf(\n  a: array f32; n: i32;\n  ipiv: array i32; info: array i32\n)\nvar\n  i, j, k, pivot, nrem, nrem4, ai_base, ak_base, joff: i32\n  t, inv, m, v: f32\n  mv, vk, vi: f32x4\nbegin\n  info[0] := 0\n  for k := 0, n\n    pivot := k\n    v := a[k, n, k]\n    t := abs(v)\n    for i := k + 1, n\n      v := a[i, n, k]\n      v := abs(v)\n      if (v > t) then\n        pivot := i\n        t := v\n      end if\n    end for\n    ipiv[k] := pivot\n\n    if (pivot /= k) then\n      for j := 0, n\n        t := a[k, n, j]\n        a[k, n, j] := a[pivot, n, j]\n        a[pivot, n, j] := t\n      end for\n    end if\n\n    if (a[k, n, k] == 0.0) then\n      info[0] := k + 1\n      call return()\n    end if\n\n    inv := 1.0 / a[k, n, k]\n    nrem := n - k - 1\n    nrem4 := nrem / 4\n    ak_base := (k * n + k + 1) * 4\n    for i := k + 1, n\n      m := a[i, n, k] * inv\n      a[i, n, k] := m\n      mv := f32x4.splat(m)\n      ai_base := (i * n + k + 1) * 4\n      for j := 0, nrem4\n        joff := j * 16\n        vk := v128.load_at(a, ak_base + joff)\n        vi := v128.load_at(a, ai_base + joff)\n        vi := f32x4.relaxed_nmadd(mv, vk, vi)\n        call v128.store_at(a, ai_base + joff, vi)\n      end for\n      for j := k + 1 + nrem4 * 4, n\n        a[i, n, j] := a[i, n, j] - m * a[k, n, j]\n      end for\n    end for\n  end for\nend",
  "alpack.sgetrs": "subroutine alpack.sgetrs(\n  lu: array f32; ipiv: array i32;\n  b: array f32; n, nrhs: i32\n)\nvar\n  rhs, i, j, p, j4, lu_row, joff: i32\n  s, t: f32\n  acc, vlu, vb: f32x4\nbegin\n  for rhs := 0, nrhs\n    for i := 0, n\n      p := ipiv[i]\n      if (p /= i) then\n        t := b[i, nrhs, rhs]\n        b[i, nrhs, rhs] := b[p, nrhs, rhs]\n        b[p, nrhs, rhs] := t\n      end if\n    end for\n\n    if (nrhs == 1) then\n      for i := 1, n\n        s := b[i]\n        j4 := i / 4\n        acc := f32x4.splat(0.0)\n        lu_row := i * n * 4\n        for j := 0, j4\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load(b, j)\n          acc := acc + vlu * vb\n        end for\n        s := s - f32x4.extract_lane(acc, 0) - f32x4.extract_lane(acc, 1) - f32x4.extract_lane(acc, 2) - f32x4.extract_lane(acc, 3)\n        for j := j4 * 4, i\n          s := s - lu[i, n, j] * b[j]\n        end for\n        b[i] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i]\n        j4 := (n - i - 1) / 4\n        acc := f32x4.splat(0.0)\n        lu_row := (i * n + i + 1) * 4\n        for j := 0, j4\n          joff := j * 16\n          vlu := v128.load_at(lu, lu_row + joff)\n          vb := v128.load_at(b, (i + 1) * 4 + joff)\n          acc := acc + vlu * vb\n        end for\n        s := s - f32x4.extract_lane(acc, 0) - f32x4.extract_lane(acc, 1) - f32x4.extract_lane(acc, 2) - f32x4.extract_lane(acc, 3)\n        for j := i + 1 + j4 * 4, n\n          s := s - lu[i, n, j] * b[j]\n        end for\n        b[i] := s / lu[i, n, i]\n      end for\n    else\n      for i := 1, n\n        s := b[i, nrhs, rhs]\n        for j := 0, i\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s\n      end for\n      for i := n - 1, -1, -1\n        s := b[i, nrhs, rhs]\n        for j := i + 1, n\n          s := s - lu[i, n, j] * b[j, nrhs, rhs]\n        end for\n        b[i, nrhs, rhs] := s / lu[i, n, i]\n      end for\n    end if\n  end for\nend",
  "alpack.sgesv": "subroutine alpack.sgesv(\n  a: array f32; b: array f32;\n  n, nrhs: i32;\n  ipiv: array i32; info: array i32\n)\nbegin\n  call alpack.sgetrf(a, n, ipiv, info)\n  if (info[0] /= 0) then\n    call return()\n  end if\n  call alpack.sgetrs(a, ipiv, b, n, nrhs)\nend",
};

export const deps = {
  "alas.dscal": [],
  "alas.dcopy": [],
  "alas.daxpy": [],
  "alas.ddot": [],
  "alas.dnrm2": [],
  "alas.dswap": [],
  "alas.dasum": [],
  "alas.drot": [],
  "alas.drotg": [],
  "alas.idamax": [],
  "alas.dgemv": [],
  "alas.dtrsv": [],
  "alas.dger": [],
  "alas.dsymv": [],
  "alas.dsyr": [],
  "alas.dsyr2": [],
  "alas.dtrmv": [],
  "alas.dgemm": [],
  "alpack.dpotrf": [],
  "alpack.dpotrs": [],
  "alpack.dgetrf": [],
  "alpack.dgetrs": [],
  "alpack.dgesv": ["alpack.dgetrf","alpack.dgetrs"],
  "alpack.dtrtri": [],
  "alpack.dsyev3": [],
  "alpack.dsyev3v": [],
  "alpack.dsyev": [],
  "alpack.dsyevv": [],
  "alas.sscal": [],
  "alas.scopy": [],
  "alas.saxpy": [],
  "alas.sdot": [],
  "alas.snrm2": [],
  "alas.sgemv": [],
  "alas.sger": [],
  "alas.sgemm": [],
  "alpack.sgetrf": [],
  "alpack.sgetrs": [],
  "alpack.sgesv": ["alpack.sgetrf","alpack.sgetrs"],
};
