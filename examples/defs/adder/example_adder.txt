/// auditable
/// title: adder — cells and tags
/// module: @gcu/adder ext/adder/index.js

/// md
# @gcu/adder

A JS-targeting Python dialect as a first-class cell language — pure JS tree-walking interpreter, no WASM. Adder cells participate in Auditable's reactive DAG — define variables in adder, use them downstream in JS or other adder cells.

/// code collapsed
// %collapsed
await load("@gcu/adder")

/// md
## basics

Assignment and last-expression output:

/// adder
x = 42
y = x + 8
y

/// md
The `y` variable is now in the DAG scope. Downstream cells see it:

/// code
display(`y from Python: ${y}`)

/// md
## functions

Python functions cross the language boundary — callable from JS:

/// adder
def greet(name):
    return "hello, " + name

/// code
display(greet("auditable"))

/// md
## print output

`print()` output appears in the cell's output area, composed with last-expression results:

/// adder
for i in range(5):
    print(f"step {i}")
"done"

/// md
## top-level await

Python cells support `await` for async JS interop:

/// adder
import js
response = await js.globalThis.fetch("data:text/plain,hello%20from%20fetch")
text = await response.text()
text

/// md
## mpy tagged template

Inline Python from JS cells using the `mpy` tag:

/// code
const result = await mpy`
a = 10
b = 20
total = a + b
`
display(`total from mpy: ${result.total}`)

/// md
## mpy with interpolation

JS values are injected via template literal interpolation:

/// code
const base = 100
const factor = 3
const calc = await mpy`
result = ${base} * ${factor}
label = f"${base} \u00d7 ${factor}"
`
display(`${calc.label} = ${calc.result}`)

/// md
## cross-language DAG

Python and JS cells interleave freely in the reactive DAG:

/// code
const data = [1, 2, 3, 4, 5]

/// adder
# values are native JS — sum() and len() work directly
total = sum(data)
mean = total / len(data)
mean

/// code
display(`mean = ${mean}`)
display(`computed in Python, displayed in JS`)
