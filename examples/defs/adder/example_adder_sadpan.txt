/// auditable
/// title: adder + sadpan — pandas-style dataframes
/// module: @gcu/adder ext/adder/index.js
/// module: @gcu/sadpan ext/sadpan/index.js

/// md
# sadpan in adder

**sadpan** registers as an auditable extension, so adder cells can
`import sadpan` directly. The `DataFrame` wrapper gives it a pandas face:
`df['col']`, `df[mask]`, `groupby().agg()`.

DataFrames implement `_repr_html_()`, so they render as tables
automatically when they're the last expression in a cell.

/// md
## create a DataFrame

/// adder
import sadpan as pd

df = pd.DataFrame({
    "hole":  ["DDH01", "DDH01", "DDH01", "DDH02", "DDH02", "DDH02", "DDH03", "DDH03"],
    "from":  [0,       10,      20,      0,       10,      20,      0,       10],
    "to":    [10,      20,      30,      10,      20,      30,      10,      20],
    "lith":  ["BIF",   "BIF",   "ITA",   "BIF",   "ITA",   "ITA",   "BIF",   "BIF"],
    "fe":    [58.2,    61.4,    48.1,    62.0,    45.3,    50.2,    59.8,    63.1],
    "au":    [0.12,    0.34,    0.08,    0.45,    0.02,    0.11,    0.28,    0.56],
})

df

/// md
## Series operators + boolean masking

`df['fe'] > 60` returns a `BooleanMask`. Pass it back to `df[mask]`
to filter rows — just like pandas.

/// adder
df[df['fe'] > 60]

/// md
## filter with lambda

/// adder
df.filter(lambda row: row['au'] > 0.2)

/// md
## compound masks

/// adder
df[(df['fe'] > 55) & (df['lith'] == "BIF")]

/// md
## groupby + agg

/// adder
df.groupby("lith").agg({"fe": "mean", "au": "sum"})

/// md
## sort values

/// adder
df.sort_values("fe", ascending=False).head(5)

/// md
## add a column

/// adder
df2 = pd.DataFrame({"x": [1, 2, 3], "y": [4, 5, 6]})
df2['z'] = df2['x'] + df2['y']
df2

/// md
## Series math

/// adder
s = df['fe']
f"mean={s.mean():.1f}, std={s.std():.1f}, min={s.min()}, max={s.max()}"

/// md
## merge (join)

/// adder
collars = pd.DataFrame({
    "hole": ["DDH01", "DDH02", "DDH03"],
    "east": [500, 520, 480],
    "north": [1000, 1015, 990],
})

df.merge(collars, on="hole").select("hole", "from", "to", "fe", "east", "north")
