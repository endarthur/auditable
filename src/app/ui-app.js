// ── UI STUBS (app runtime) ──

function setMsg(msg, cls) {
  if (cls === 'err') console.error('[auditable]', msg);
}

function updateStatus() {}
function updateInsertBars() {}
function insertAt() {}
