# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Build intermediates (uncompressed auditable.html / works.html)
/build/

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.*
!.env.example

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist
.output

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# pnpm
.pnpm-store

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/

# Claude files
.claude/
CLAUDE.md
ROADMAP.md

# MkDocs build output
site/

# Specs to be ingested
spec_inbox/

# Personal notebooks
notebooks/

# Ad-hoc experiments and benchmarks (compression, perf, scratch)
experiments/

# rv emulator — reference binaries (not ours to distribute)
ext/rv/ref/mini-rv32ima.exe
ext/rv/ref/our.dtb
.kdtree-bench-data/

# Project Gutenberg source dumps for the librarian demo — re-fetched
# by examples/defs/data-corpora/build.js. The generated corpus modules
# at examples/data/{sherlock,aesop}.js ARE committed.
examples/defs/data-corpora/*.raw.txt

# @gcu/make incremental build cache
.gcu-make.cache.json

# Landing toy bundles — copied from ext/ for local preview; CI deploys them to gh-pages /lib/
landing/lib/

# Generated distributables — built by CI (release.yml) → gh-pages + release assets, so
# they don't bloat .git. auditable.html stays committed (the product + the update root).
/works.html
/works-core.html
/works-all.html
/editions/*.html
# First-party package catalog — generated by `node build.js --target=packages`,
# emitted to the deploy (auditable hosts its own .gcupkg artifacts + registry.json).
/packages/
examples/**/*.html
examples/**/*.recovery.txt

# gcu-make run: recipe demo output (wasm4-cart target in make.yaml) — a
# demonstration artifact regenerated from ext/wasm4/cart-demo.atra.
ext/wasm4/build/

# gsjs generated atra→wasm bundle (regenerated by ext/gsjs/build.js)
ext/gsjs/src/_wasm.js
lamina.html
micro.html
moncad.html
tools/micro/docs/shots/
